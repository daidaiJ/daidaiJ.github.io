<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Mcp on 潘达窝</title><link>https://daidaij.github.io/tags/mcp/</link><description>Recent content in Mcp on 潘达窝</description><generator>Hugo -- gohugo.io</generator><language>zh-cn</language><copyright>pandazhangs</copyright><lastBuildDate>Sat, 12 Sep 2026 09:19:18 +0800</lastBuildDate><atom:link href="https://daidaij.github.io/tags/mcp/index.xml" rel="self" type="application/rss+xml"/><item><title>websearch-mcpserver 工具链：检索之外的三件套</title><link>https://daidaij.github.io/p/websearch-mcpserver-toolchain/</link><pubDate>Sat, 12 Sep 2026 09:19:18 +0800</pubDate><guid>https://daidaij.github.io/p/websearch-mcpserver-toolchain/</guid><description>&lt;img src="https://picsum.photos/seed/7a4e1ac3/800/600" alt="Featured image of post websearch-mcpserver 工具链：检索之外的三件套" />&lt;h1 id="websearch-mcpserver-工具链检索之外的三件套">websearch-mcpserver 工具链：检索之外的三件套
&lt;/h1>&lt;hr>
&lt;blockquote>
&lt;p>上一篇写了聚合的四次重做（smartsearch 那条线）。这篇写剩下三个工具——academicsearch、cleanfetch、pdf_parser——和兜着它们的支撑件。单独一个 smartsearch 只解决&amp;quot;搜到&amp;quot;，agent 实际要的是闭环：搜到 → 打开 → 读进去，缺哪环都得自己手搓。&lt;/p>
&lt;/blockquote>
&lt;h2 id="四个工具怎么分工">四个工具怎么分工
&lt;/h2>&lt;p>工具注册在 &lt;code>mcp/server.go&lt;/code>，条件化注册 + 动态描述是两个通用设计：&lt;/p>
&lt;ul>
&lt;li>smartsearch：LLM 配了就注册带 &lt;code>intent&lt;/code> 参数的版本（agent 声明检索目的换更准的结构化摘要），没配就注册无 intent 版&lt;/li>
&lt;li>cleanfetch：需显式启用才注册&lt;/li>
&lt;li>pdf_parser：默认关闭，工具描述按配置动态生成——配了 MinerU OCR 就多一句&amp;quot;扫描件可回退 OCR&amp;quot;，只配了 Token 就只说远程精准解析&lt;/li>
&lt;/ul>
&lt;p>动态描述这个点值得说一下：&lt;strong>agent 看到的工具描述永远和实际能力一致&lt;/strong>，不会出现&amp;quot;描述说支持 OCR、实际没配 Token 调了报错&amp;quot;的错位。&lt;/p>
&lt;h2 id="academicsearch九引擎垂直线">academicsearch：九引擎垂直线
&lt;/h2>&lt;p>学术检索是独立的一条垂直线：arXiv、PubMed、Europe PMC、DBLP、DOAJ、Crossref、Semantic Scholar、Google Scholar、OpenAlex 九个引擎并行，全部国内可直连或可代理。&lt;/p>
&lt;p>评分在通用 Wigolo 之上叠学术特有信号，&lt;code>pkg/search/academic_enhance.go&lt;/code>：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt"> 1
&lt;/span>&lt;span class="lnt"> 2
&lt;/span>&lt;span class="lnt"> 3
&lt;/span>&lt;span class="lnt"> 4
&lt;/span>&lt;span class="lnt"> 5
&lt;/span>&lt;span class="lnt"> 6
&lt;/span>&lt;span class="lnt"> 7
&lt;/span>&lt;span class="lnt"> 8
&lt;/span>&lt;span class="lnt"> 9
&lt;/span>&lt;span class="lnt">10
&lt;/span>&lt;span class="lnt">11
&lt;/span>&lt;span class="lnt">12
&lt;/span>&lt;span class="lnt">13
&lt;/span>&lt;span class="lnt">14
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// CiteFactor 引用数增强因子：对数压缩避免高引论文完全碾压，clamp 到 [1.0, 1.7]。
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="kd">func&lt;/span> &lt;span class="nf">CiteFactor&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">citedBy&lt;/span> &lt;span class="kt">int&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="kt">float64&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="nx">citedBy&lt;/span> &lt;span class="o">&amp;lt;=&lt;/span> &lt;span class="mi">0&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="mf">1.0&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">f&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="o">+&lt;/span> &lt;span class="nx">math&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Log2&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="mi">1&lt;/span>&lt;span class="o">+&lt;/span>&lt;span class="nb">float64&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">citedBy&lt;/span>&lt;span class="p">))&lt;/span>&lt;span class="o">*&lt;/span>&lt;span class="mf">0.05&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="nx">f&lt;/span> &lt;span class="p">&amp;lt;&lt;/span> &lt;span class="mf">1.0&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="mf">1.0&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="nx">f&lt;/span> &lt;span class="p">&amp;gt;&lt;/span> &lt;span class="mf">1.7&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="mf">1.7&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nx">f&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>对数压缩 + clamp 这两个动作缺一不可：不压缩，一篇 Nature 引用 5 万次直接碾压所有结果；不 clamp，低引论文永远没有出头日。之外还有 JournalBoost（高影响力期刊/会议加性加分，大小写不敏感查表）、PDF 全文可用性、时效因子（时间敏感查询近一年 ×1.15）。&lt;/p>
&lt;p>去重用 DOI + URL 双键合并——&amp;ldquo;一方有 DOI、一方只有相同 URL&amp;quot;的同文不合并是漏报重灾区，双键任一命中即视为同文。&lt;/p>
&lt;p>对上游的防御分三层：403/429 指数退避（1.5s→3s 带随机抖动）并轮换桌面 UA；CAPTCHA（Google Scholar 的 /sorry 跳转）识别后不浪费重试直接报错；Semantic Scholar 带 Key 遇 429/503 退避，连续失败自动降级匿名模式。每家的 &lt;code>time_range&lt;/code> 语法还各不相同（Crossref 用 &lt;code>filter=from-pub-date:&lt;/code>、DOAJ 用年份闭区间、arXiv 用 UTC 时间戳区间），v3.4.0 把三家各修了一遍。&lt;/p>
&lt;p>最后是错误透传：部分引擎失败不静默吞掉，失败引擎记入 &lt;code>EngineErrors&lt;/code>，结果末尾提示&amp;quot;部分引擎本次失败，结果可能不完整&amp;rdquo;——agent 知道结果的置信度边界，比拿到一份看起来完整的半成品强。&lt;/p>
&lt;h2 id="cleanfetch抓取的降级链与安全边界">cleanfetch：抓取的降级链与安全边界
&lt;/h2>&lt;p>cleanfetch 解决&amp;quot;搜到了，打开看看&amp;quot;。抓取前先过两道安全闸：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>DNS rebinding 防护&lt;/strong>：抓取前解析目标域名，检查所有解析出的 IP 是否内网/私有地址，和 go-webfetch 内部的 BlockPrivateIP 构成双重防护——agent 是会接受模型给的任意 URL 的，SSRF 面必须按&amp;quot;URL 不可信&amp;quot;设计&lt;/li>
&lt;li>&lt;strong>HEAD 预检&lt;/strong>：先看 Content-Length，超过 &lt;code>max_fetch_size_mb&lt;/code>（默认 10MB）直接拒绝&lt;/li>
&lt;/ol>
&lt;p>抓取本体是三层降级：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// 第一层：go-webfetch（无需代理）
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="nx">result&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">err&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="nx">webfetchInst&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Fetch&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">ctx&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">params&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">URL&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="k">if&lt;/span> &lt;span class="nx">err&lt;/span> &lt;span class="o">!=&lt;/span> &lt;span class="kc">nil&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">out&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">ferr&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="nf">fetchFallbacks&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">ctx&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">params&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">URL&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">err&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="c1">// Jina → 浏览器兜底
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>go-webfetch 用 tls-client 做 Chrome 131 TLS 指纹伪装（反爬的第一道坎是指纹不是 UA），失败回退 Jina Reader，再不行可选用户自备浏览器命令。关键是 &lt;code>fetchFallbacks&lt;/code> 把每一层的失败原因聚合透传（&lt;code>webfetch: ...; Jina: ...&lt;/code>）——agent 看得到&amp;quot;哪层为什么没成&amp;quot;，而不是一个笼统的 failed。&lt;/p>
&lt;h2 id="pdf_parser本地优先ai-按需">pdf_parser：本地优先、AI 按需
&lt;/h2>&lt;p>PDF 解析走的是成本敏感的两段式：本地 PDF 库先抽文本（免费、毫秒级），抽不动（扫描件、图片型 PDF）才按需回退 MinerU OCR（要 Token、要上传、要等）。&lt;code>pkg/webfetch/webfetch.go&lt;/code>：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// 本地 PDF 文件：先本地 PDF 库抽文本，读不到再按需走 MinerU OCR
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="k">if&lt;/span> &lt;span class="nx">strings&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">HasPrefix&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">rawURL&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;file://&amp;#34;&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">localPath&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="nx">strings&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">TrimPrefix&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">rawURL&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;file://&amp;#34;&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1">// 处理 Windows 三斜杠格式 file:///C:/...
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span> &lt;span class="o">...&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nx">f&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">parseLocalPDF&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">ctx&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">localPath&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>连 Windows 三斜杠的 &lt;code>file:///C:/...&lt;/code> 都单独处理了——agent 给的路径格式从来不讲武德。大文档自动落临时文件而不是塞进工具结果，避免一次解析吃掉半个上下文窗口。&lt;/p>
&lt;h2 id="go-webfetch从项目里长出来的独立模块">go-webfetch：从项目里长出来的独立模块
&lt;/h2>&lt;p>网页抓取做到后来发现和搜索一样有通用价值，抽成了独立 Go module（&lt;code>github.com/daidaiJ/go-webfetch&lt;/code>，v0.2.0）发布，主仓库按版本引用。这是这个项目第二次&amp;quot;长出独立资产&amp;quot;——第一次是把 server 包做成可嵌入模块。&lt;/p>
&lt;h2 id="server-包两种嵌入形态">server 包：两种嵌入形态
&lt;/h2>&lt;p>&lt;code>server&lt;/code> 包把整个服务做成可复用组件：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="nx">srv&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="nx">server&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">New&lt;/span>&lt;span class="p">()&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nx">srv&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Run&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="o">*&lt;/span>&lt;span class="nx">conf&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="c1">// 完整托管：引擎、MCP 路由、缓存清理协程、信号处理
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="nx">handler&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="nx">srv&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Handler&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="o">*&lt;/span>&lt;span class="nx">conf&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="c1">// 只要 http.Handler，嵌入已有服务的端口/TLS/中间件栈
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>配合引用计数 daemon（start/stop/kill/status，归零自动优雅退出）和 MCP hooks 的 SessionStart/SessionEnd，多会话共享实例、全关自动停。SearXNG 模式的路由也还留着——第一版没白做，它成了一个可选的引擎源。&lt;/p>
&lt;h2 id="发布矩阵">发布矩阵
&lt;/h2>&lt;p>分发按三个渠道拆：GitHub Release 四平台二进制（linux/windows amd64 + darwin amd64/arm64）、GHCR 镜像（linux/amd64+arm64）、MCP Registry 的 mcpb 包。tag 约定是先打 &lt;code>vX.Y.Z&lt;/code> 发 Release 推镜像，发完再单独补 &lt;code>-registry&lt;/code> 后缀 tag 发 MCP Registry（钉同一 commit）——这个顺序是发坏一次学出来的，混在一起推会在 Registry 侧拿到还没构建完的产物。&lt;/p>
&lt;h2 id="小结">小结
&lt;/h2>&lt;p>三个工具串起来是一条&amp;quot;搜到 → 打开 → 读进去&amp;quot;的闭环，每个环节都遵循同一套原则：降级链分层且错误透传、安全边界按&amp;quot;输入不可信&amp;quot;设计、贵的能力（OCR、LLM）永远放在按需位置。加上聚合篇的四版演进，这个项目到目前为止最有复用价值的可能不是任何一个具体引擎，而是这套&amp;quot;任何环节都会失败&amp;quot;的组织方式。&lt;/p></description></item><item><title>websearch-mcpserver 复盘：聚合为什么重做了四次</title><link>https://daidaij.github.io/p/websearch-mcpserver-evolution/</link><pubDate>Sat, 12 Sep 2026 09:19:17 +0800</pubDate><guid>https://daidaij.github.io/p/websearch-mcpserver-evolution/</guid><description>&lt;img src="https://picsum.photos/seed/e268d8e8/800/600" alt="Featured image of post websearch-mcpserver 复盘：聚合为什么重做了四次" />&lt;h1 id="websearch-mcpserver-复盘聚合为什么重做了四次">websearch-mcpserver 复盘：聚合为什么重做了四次
&lt;/h1>&lt;hr>
&lt;blockquote>
&lt;p>自建检索服务 2026-03-20 init，六个月 78 个 commit，Go 实现。聚合设计重做了四次，没有一次是计划好的——每次都是撞上一个靠不住的东西才动手。这篇按&amp;quot;是什么靠不住&amp;quot;组织，同时把核心模块的真实实现放进来：引擎接口、KeyPool、apipool、Wigolo 评分管线。工具链的三件套（academicsearch / cleanfetch / pdf_parser）另成一篇。&lt;/p>
&lt;/blockquote>
&lt;h2 id="前提三个约束">前提：三个约束
&lt;/h2>&lt;ol>
&lt;li>国内网络下 Tavily/Exa 这些商业 API 时通时断，Google/DDG 直连基本没戏，代理还得自动检测&lt;/li>
&lt;li>agent 检索是高频操作，付费额度烧得飞快，手里只有各家散落的免费额度&lt;/li>
&lt;li>搜索结果对 agent 不友好（无摘要、无元数据、条数失控）的话，一次检索吃掉半个上下文窗口&lt;/li>
&lt;/ol>
&lt;h2 id="第一版重做聚合器靠不住">第一版重做：聚合器靠不住
&lt;/h2>&lt;p>第一版接 searxng，聚合、去重、排序都现成，我只写 MCP 接口的壳。但它把三样东西锁死了：多一个实例的运维、上游引擎在国内的可达性、排序黑盒没法按 agent 场景定制。&lt;/p>
&lt;p>结论：聚合逻辑是这个项目唯一的核心竞争力，不能外包。于是有了后面所有版本的地基——&lt;/p>
&lt;h2 id="地基一个-4-方法的引擎接口">地基：一个 4 方法的引擎接口
&lt;/h2>&lt;p>所有引擎（百度、Bing、Google、DDG、Tavily、Exa、AnySearch、豆包、9 个学术引擎）实现同一个接口，&lt;code>pkg/search/inf.go&lt;/code>：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="kd">type&lt;/span> &lt;span class="nx">SearchInf&lt;/span> &lt;span class="kd">interface&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nf">Name&lt;/span>&lt;span class="p">()&lt;/span> &lt;span class="kt">string&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nf">Search&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">query&lt;/span> &lt;span class="kt">string&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="kt">string&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="kt">error&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="c1">// 聚合后的文本结果（喂模型）
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span> &lt;span class="nf">SearchRaw&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">query&lt;/span> &lt;span class="kt">string&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="p">([]&lt;/span>&lt;span class="nx">SearchResult&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="kt">error&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="c1">// 结构化结果（进评分管线）
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span> &lt;span class="nf">MergeContent&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">query&lt;/span> &lt;span class="kt">string&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">results&lt;/span> &lt;span class="p">[]&lt;/span>&lt;span class="nx">SearchResult&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="kt">string&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="kt">error&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>接口刻意做小，引擎差异靠两个机制吸收：可选能力用单独的小接口（时间范围是 &lt;code>SearchTimeRanger&lt;/code>，实现了才有；豆包的 Custom 端点实现了它）；各家 score 不可比的问题不管——结构化结果只带原始排名，可比性留给评分管线。&lt;/p>
&lt;p>后面加一个引擎的成本是：写一个文件 + factory 一行注册。这个地基决定了后面三次重做都不用推倒接口层。&lt;/p>
&lt;h2 id="第二版重做单家引擎靠不住">第二版重做：单家引擎靠不住
&lt;/h2>&lt;p>直连各引擎自己聚合（&lt;code>e74e427&lt;/code>、&lt;code>f0d9a33&lt;/code>、&lt;code>5890e66&lt;/code>）。多引擎并发、单家失败不阻塞；降级链成型：每个模式主引擎失败自动回退，无 Key 自动降级免费引擎。降级链不是容错补丁，是产品形态本身。&lt;/p>
&lt;h2 id="第三版重做免费额度靠不住--keypool--apipool">第三版重做：免费额度靠不住 —— KeyPool + apipool
&lt;/h2>&lt;p>手里散着 anysearch、百度千帆、Tavily、Exa、豆包的 Key，每家免费额度单用不够烧。apipool 每次只调一个供应商，失败自动切换，同一供应商内先试完所有 SK 再换下一家。&lt;/p>
&lt;p>两个实现细节值得展开。&lt;/p>
&lt;p>&lt;strong>KeyError 精确失效&lt;/strong>。请求失败时要标记&amp;quot;刚才用的那个 Key&amp;quot;失效，但错误在多层传播后容易把 Key 带进日志。实现是一个专用的 error 包装，&lt;code>pkg/search/apipool.go&lt;/code>：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt"> 1
&lt;/span>&lt;span class="lnt"> 2
&lt;/span>&lt;span class="lnt"> 3
&lt;/span>&lt;span class="lnt"> 4
&lt;/span>&lt;span class="lnt"> 5
&lt;/span>&lt;span class="lnt"> 6
&lt;/span>&lt;span class="lnt"> 7
&lt;/span>&lt;span class="lnt"> 8
&lt;/span>&lt;span class="lnt"> 9
&lt;/span>&lt;span class="lnt">10
&lt;/span>&lt;span class="lnt">11
&lt;/span>&lt;span class="lnt">12
&lt;/span>&lt;span class="lnt">13
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// KeyError 携带请求失败时实际使用的 API Key，供 apipool 精确标记失效。
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// 注意：Error() 不输出 Key 本身，避免 API Key 泄漏到日志。
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="kd">type&lt;/span> &lt;span class="nx">KeyError&lt;/span> &lt;span class="kd">struct&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">Key&lt;/span> &lt;span class="kt">string&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">Err&lt;/span> &lt;span class="kt">error&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="kd">func&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="nx">e&lt;/span> &lt;span class="o">*&lt;/span>&lt;span class="nx">KeyError&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="nf">Error&lt;/span>&lt;span class="p">()&lt;/span> &lt;span class="kt">string&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="nx">e&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">Err&lt;/span> &lt;span class="o">==&lt;/span> &lt;span class="kc">nil&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="s">&amp;#34;key error&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nx">e&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">Err&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Error&lt;/span>&lt;span class="p">()&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>&lt;strong>KeyPool 冷却自愈&lt;/strong>。Key 失效不是永久的（限流、欠费都会恢复），所以不是删掉而是打 30 分钟冷却戳，&lt;code>pkg/search/keypool.go&lt;/code>：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt"> 1
&lt;/span>&lt;span class="lnt"> 2
&lt;/span>&lt;span class="lnt"> 3
&lt;/span>&lt;span class="lnt"> 4
&lt;/span>&lt;span class="lnt"> 5
&lt;/span>&lt;span class="lnt"> 6
&lt;/span>&lt;span class="lnt"> 7
&lt;/span>&lt;span class="lnt"> 8
&lt;/span>&lt;span class="lnt"> 9
&lt;/span>&lt;span class="lnt">10
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// 轮转游标原子递增，跳过冷却中的 key
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="k">for&lt;/span> &lt;span class="nx">i&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="nb">uint64&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="mi">0&lt;/span>&lt;span class="p">);&lt;/span> &lt;span class="nx">i&lt;/span> &lt;span class="p">&amp;lt;&lt;/span> &lt;span class="nx">n&lt;/span>&lt;span class="p">;&lt;/span> &lt;span class="nx">i&lt;/span>&lt;span class="o">++&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">idx&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="nx">p&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">idx&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Add&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="o">-&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="o">%&lt;/span> &lt;span class="nx">n&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">if&lt;/span> &lt;span class="nx">p&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">states&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="nx">idx&lt;/span>&lt;span class="p">].&lt;/span>&lt;span class="nx">invalidUntil&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">IsZero&lt;/span>&lt;span class="p">()&lt;/span> &lt;span class="o">||&lt;/span> &lt;span class="nx">now&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">After&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">p&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">states&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="nx">idx&lt;/span>&lt;span class="p">].&lt;/span>&lt;span class="nx">invalidUntil&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">p&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">states&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="nx">idx&lt;/span>&lt;span class="p">].&lt;/span>&lt;span class="nx">invalidUntil&lt;/span> &lt;span class="p">=&lt;/span> &lt;span class="nx">time&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">Time&lt;/span>&lt;span class="p">{}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nx">p&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">keys&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="nx">idx&lt;/span>&lt;span class="p">]&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="o">...&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// 全部失效，返回最早恢复的那个（不等待）
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>轮转指针原子递增，冷却到期的 key 自动回池；全部失效时返回最早恢复的那个而不是直接报错——配合百度网页搜索兜底，池子任何情况下不空转。weighted 策略下供应商有效权重 = 配置权重 × 当前可用 SK 数，Key 冷却权重自动下降、恢复自愈。&lt;/p>
&lt;h2 id="第四版重做混合结果靠不住--wigolo-管线">第四版重做：混合结果靠不住 —— Wigolo 管线
&lt;/h2>&lt;p>hybrid 模式全引擎并发后，新问题是 9 个引擎的结果混在一起，转载站、镜像站、SEO 农场霸屏。这条线最终长成了完整的本地评分管线，全程纯启发式——搜索本身不能再引入一次 LLM 调用的成本和延迟。&lt;/p>
&lt;p>编排层 &lt;code>HybridSearchImpl&lt;/code> 支持按引擎配置过滤：每个引擎可以单独设最低分、单引擎条数上限、引擎权重（影响 RRF 融合分）：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="kd">type&lt;/span> &lt;span class="nx">engineFilter&lt;/span> &lt;span class="kd">struct&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">minScore&lt;/span> &lt;span class="kt">float64&lt;/span> &lt;span class="c1">// 最低相关性分数，0 = 不过滤
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span> &lt;span class="nx">maxSize&lt;/span> &lt;span class="kt">int&lt;/span> &lt;span class="c1">// 单引擎最大结果数，0 = 使用默认值
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span> &lt;span class="nx">weight&lt;/span> &lt;span class="kt">float64&lt;/span> &lt;span class="c1">// 引擎权重，影响 RRF 融合分，0 = 默认 1.0
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>核心是 RRF（Reciprocal Rank Fusion），&lt;code>pkg/search/enhance.go&lt;/code>：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;span class="lnt">8
&lt;/span>&lt;span class="lnt">9
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="kd">const&lt;/span> &lt;span class="nx">rrfK&lt;/span> &lt;span class="p">=&lt;/span> &lt;span class="mf">60.0&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="kd">func&lt;/span> &lt;span class="nf">RRFScore&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">ranks&lt;/span> &lt;span class="kd">map&lt;/span>&lt;span class="p">[&lt;/span>&lt;span class="kt">string&lt;/span>&lt;span class="p">]&lt;/span>&lt;span class="kt">int&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">K&lt;/span> &lt;span class="kt">float64&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="kt">float64&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="kd">var&lt;/span> &lt;span class="nx">score&lt;/span> &lt;span class="kt">float64&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">for&lt;/span> &lt;span class="nx">_&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">rank&lt;/span> &lt;span class="o">:=&lt;/span> &lt;span class="k">range&lt;/span> &lt;span class="nx">ranks&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">score&lt;/span> &lt;span class="o">+=&lt;/span> &lt;span class="mf">1.0&lt;/span> &lt;span class="o">/&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="nx">K&lt;/span> &lt;span class="o">+&lt;/span> &lt;span class="nb">float64&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">rank&lt;/span>&lt;span class="p">))&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nx">score&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>只比排名不比分数——第二版做过分数归一化，但各家 score 口径永远做不齐，RRF 从根上绕开。融合之上叠四层信号：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>词汇对齐&lt;/strong>：查询词与标题/内容的词级匹配，停用词表是中英混合场景自己攒的（连 latest/current 这种检索场景特有的口水词都进去了），稀有词和连续短语加权&lt;/li>
&lt;li>&lt;strong>域名品质惩罚&lt;/strong>：品牌/电商/词典站误匹配降权（独立成 &lt;code>enhance_domain.go&lt;/code>）&lt;/li>
&lt;li>&lt;strong>共识/权威/时效加分&lt;/strong>：多引擎都返回的结果天然更可信，&lt;code>ConsensusBoost&lt;/code> 按引擎数加性加分&lt;/li>
&lt;li>&lt;strong>低分阈值过滤 + MMR 贪心重排&lt;/strong>：Token Jaccard 相似度，把转载站和同源博客打散&lt;/li>
&lt;/ol>
&lt;p>学术侧是同一套思路的变体，放到工具链那篇细说。&lt;/p>
&lt;h2 id="检索之上意图摘要与缓存">检索之上：意图、摘要与缓存
&lt;/h2>&lt;p>smartsearch 的 LLM 集成是可选的，注册逻辑跟着配置走：LLM 开着就注册带 &lt;code>intent&lt;/code> 参数的版本——agent 可以声明检索目的（查资料/找代码/看新闻），后端按意图调摘要策略；没配 LLM 就注册无 intent 版本，功能不缺。&lt;/p>
&lt;p>流式摘要的降级链：LLM 流式摘要失败 → 非流式摘要 → 原始结果直接返回。摘要永远不是必需品，是锦上添花。&lt;/p>
&lt;p>缓存用 SQLite（modernc.org/sqlite 纯 Go 驱动，不引 CGO，单二进制能保住），WAL 模式，6 小时过期按最近命中时间算，30 分钟定时清理，学术和非学术结果按参数区分防混用。缓存查询异常就跳过缓存直接搜——缓存也是会坏的，它坏不能拖累主链路。&lt;/p>
&lt;h2 id="一直都在的暗线网络环境靠不住">一直都在的暗线：网络环境靠不住
&lt;/h2>&lt;p>散落在所有 commit 里的一条线。系统代理检测做了三层：Windows 注册表（ProxyEnable/ProxyServer）+ WinHTTP + 环境变量，后台 30 秒轮询，&lt;code>DynamicProxyTransport&lt;/code> 请求级动态解析——Clash/V2RayN 开关系统代理不用重启服务，下一个请求自动跟上。加上 DDG 仅在代理可用时进引擎池、Google 默认禁用、Google Scholar 403/429 指数退避加轮换 UA、CAPTCHA 识别后直接报错不浪费重试、arXiv 内置 1 req/s 限流器。&lt;/p>
&lt;p>结论：引擎可用性不是布尔值，是&amp;quot;在哪个网络环境下&amp;quot;的函数，配置里每个开关背后都是一次实测。&lt;/p>
&lt;h2 id="无状态开关">无状态开关
&lt;/h2>&lt;p>2026-07-28 MCP 规范修订把无状态定为方向，8 月底的 v3.2.1（&lt;code>f46b091&lt;/code>）跟着加了 &lt;code>mcp_stateless&lt;/code> 开关：每个 POST 独立处理，免 initialize 握手与 &lt;code>Mcp-Session-Id&lt;/code>，GET SSE 长连直接 405。对这个服务改造成本为零——工具全是请求-响应式，会话本来就用不上。&lt;/p>
&lt;h2 id="复盘小结">复盘小结
&lt;/h2>&lt;p>四次重做不是互相推翻，是上一版把某类问题解决干净后暴露出下一类。这个项目对&amp;quot;失败&amp;quot;的处理已经形成固定套路：引擎失败有降级链、Key 失败有冷却池、供应商死光有百度兜底、摘要失败回退原始结果、缓存坏了跳过——设计里没有假设任何东西可靠。工具链的三件套（academicsearch / cleanfetch / pdf_parser）和安全设计在工具链篇。&lt;/p></description></item><item><title>上下文减法（一）：压缩工具输出的三条路线</title><link>https://daidaij.github.io/p/ctx-lean-1-pipeline/</link><pubDate>Sat, 12 Sep 2026 08:52:46 +0800</pubDate><guid>https://daidaij.github.io/p/ctx-lean-1-pipeline/</guid><description>&lt;img src="https://picsum.photos/seed/7b2e4f9a/800/600" alt="Featured image of post 上下文减法（一）：压缩工具输出的三条路线" />&lt;h1 id="上下文减法一压缩工具输出的三条路线">上下文减法（一）：压缩工具输出的三条路线
&lt;/h1>&lt;hr>
&lt;blockquote>
&lt;p>agent 干活的成本大头是工具输出：一次构建日志、一份搜索结果、一次 grep，动辄几千 token 进上下文。这半年陆续用了三个从&amp;quot;输出进上下文之前&amp;quot;下手的工具：rtk（命令执行点）、headroom（API 传输层）、context-mode（工具封装层），自己也从 headroom fork 过一个压缩库（only-cc-lite）。这篇按拦截位置整理三条路线的差异，重点是各自的失效方式——压缩率是宣传数字，怎么失效才决定敢不敢用。&lt;/p>
&lt;/blockquote>
&lt;h2 id="路线一rtk在命令执行点包一层">路线一：rtk，在命令执行点包一层
&lt;/h2>&lt;p>rtk 是 Rust 写的 CLI 代理，通过 hook 把 agent 的命令调用包一层，输出进上下文前先压缩，go build / go test / golangci-lint 这类高噪声输出正好是它压缩率最高的场景（60-90%）。&lt;/p>
&lt;p>优点是最早见效：不改架构，装个 hook 全量生效。&lt;/p>
&lt;p>但我实际用下来踩到一个比费 token 更麻烦的坑：rtk 把 &lt;code>go build&lt;/code> 改写成自己的包装调用后，输出误导性的 &amp;ldquo;Go build: Success&amp;rdquo;——而真实退出码可能仍是非零（卡巴斯基实时扫描锁 go 链接产物导致的偶发失败，就被这个 Success 盖住了）。agent 拿到 Success 就走下一步，错误被静默吞掉，只能靠&amp;quot;看产物文件在不在&amp;quot;兜底。&lt;/p>
&lt;blockquote>
&lt;p>这类工具的通用风险：在压缩输出的时候顺手改变了输出语义。对 agent 来说，退出码和报错文本是它判断世界的全部依据，格式压缩可以，语义说谎不行。&lt;/p>
&lt;/blockquote>
&lt;p>社区这边的数据更难看。JetBrains 2026 年 7 月做过一次独立基准，把 Claude Code 挂上 rtk 跑真实任务，&lt;strong>成本中位数反而增加 7.6%&lt;/strong>，和 60-90% 的宣传完全相反；Quesma 的分析同样得出基准对不上结论。HN 有个帖子标题就叫 &amp;ldquo;The Token Compression Illusion&amp;rdquo;，评论区推荐 headroom 的理由是它&amp;quot;在仓库里提供准确性基准，压缩的透明度更高&amp;quot;。有意思的是 rtk 的 README 现在自己改了口径：&lt;em>&amp;ldquo;RTK cuts up to 90% of the bash output your agent reads. That is what RTK measures, and it is not the same as cutting your bill by 90%.&amp;quot;&lt;/em>——压掉的输出 token 不等于省掉的账单，账单里还有系统提示、历史和缓存折扣。这个赛道甚至长出了反向工具：TokenTrust 是一个验证层，把 rtk/headroom 这类代理当真实进程跑，检查压缩有没有丢掉任务必需的内容、宣传的节省能否复现。一个赛道出现了专门的打假工具，本身就说明宣传数字该打折听。&lt;/p>
&lt;h2 id="路线二headroom在-api-传输层做代理">路线二：headroom，在 API 传输层做代理
&lt;/h2>&lt;p>headroom 把代理架在 agent 和模型 API 之间（&lt;code>ANTHROPIC_BASE_URL&lt;/code> 指过去），压缩面覆盖工具输出、日志、RAG 分块、文件内容甚至会话历史，口径 60-95%。两个不错的设计：原始内容本地保留可恢复（压缩错了有得救）；多 agent 共享同一份压缩上下文（Claude 和 Codex 并排跑时不用各压一份）。&lt;/p>
&lt;p>社区口碑上 headroom 相对占优，理由就是上面那句&amp;quot;提供准确性基准、透明度更高&amp;rdquo;。但它的失效方式同样隐蔽：issue #746 记录 Claude Code 走这个代理后，原本的 deferred tool schemas（工具目录按需加载）行为失效，工具 schema 全量进上下文。传输层介入改了 agent 的自我优化行为，省下来的输出 token 可能被工具目录的增量吃回去。&lt;/p>
&lt;h2 id="only-cc-litefork-只留压缩内核">only-cc-lite：fork 只留压缩内核
&lt;/h2>&lt;p>only-cc-lite 是我从 headroom 抽出来的压缩库。动机：headroom 完整栈带着 ONNX Runtime、fastembed、HuggingFace tokenizers 这些 ML 依赖，而压缩内核本身是纯启发式的——JSON 数组 60-90%、日志按 ERROR/WARN 正则 50-80%、diff 40-60%。&lt;/p>
&lt;p>fork 后零 ML 依赖，token 计数用字符密度估算（按模型族校准 chars/token），单函数调用就能嵌进 HTTP 代理中间层：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-rust" data-lang="rust">&lt;span class="line">&lt;span class="cl">&lt;span class="k">use&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="n">only_cc_lite&lt;/span>::&lt;span class="p">{&lt;/span>&lt;span class="n">compress_request&lt;/span>&lt;span class="p">,&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="n">Provider&lt;/span>&lt;span class="p">};&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="w">&lt;/span>&lt;span class="kd">let&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">compressed&lt;/span>&lt;span class="p">,&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="n">metrics&lt;/span>&lt;span class="p">)&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="n">compress_request&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">body&lt;/span>&lt;span class="p">,&lt;/span>&lt;span class="w"> &lt;/span>&lt;span class="n">Provider&lt;/span>::&lt;span class="n">Claude&lt;/span>&lt;span class="p">);&lt;/span>&lt;span class="w">
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>这个 fork 给我一个判断：这类工具真正的资产是&amp;quot;哪些格式能压、怎么压&amp;quot;的规则库，不是模型。ML 依赖带来的语义能力，在这个场景的收益撑不起它的部署重量。&lt;/p>
&lt;h2 id="路线三context-mode在工具封装层">路线三：context-mode，在工具封装层
&lt;/h2>&lt;p>context-mode（本周 trending 上的项目）走得更激进：MCP server + hooks 拦截工具调用，输出先进沙箱，主 agent 拿到的是一份号称压缩 98% 的摘要。&lt;/p>
&lt;p>机制上和前两条不是一个物种：工具输出先进本地 SQLite 的 FTS5 索引（分块 + BM25 排序 + Porter 词干化），主 agent 拿短摘要，要细节时自己检索存储。作者在 HN 的表述很准——它是&amp;quot;不让冗余进来，而不是进来之后再剪掉&amp;quot;，和压缩已有输出是两种哲学。98% 这个数字在社交媒体上到处在转，但注意口径：省的是&amp;quot;工具输出&amp;quot;的阅读，不是会话总账单。&lt;/p>
&lt;p>代价有两层。信息层：主 agent 看不到索引外的细节，它甚至不知道自己漏了什么——格式压缩的错误是&amp;quot;少了几行&amp;quot;，摘要检索的错误是&amp;quot;结论就是错的&amp;quot;，后者不留痕迹。开销层：社区有用户实测发现它往每轮系统提示里注入约 15k token 的说明，轻负载下这部分能吃掉省下的量。每个走这条路线的工具都该被问一句：你的固定开销是多少？&lt;/p>
&lt;h2 id="三条路线对比">三条路线对比
&lt;/h2>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>路线&lt;/th>
&lt;th>拦截位置&lt;/th>
&lt;th>保真度&lt;/th>
&lt;th>失效方式&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>rtk&lt;/td>
&lt;td>命令执行点&lt;/td>
&lt;td>格式压缩，但可能改语义&lt;/td>
&lt;td>误导性成功&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>headroom&lt;/td>
&lt;td>API 传输层&lt;/td>
&lt;td>格式压缩 + 可恢复&lt;/td>
&lt;td>干扰 agent 自身优化行为&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>context-mode&lt;/td>
&lt;td>工具封装层&lt;/td>
&lt;td>沙箱索引 + 按需检索&lt;/td>
&lt;td>细节不可见且无感知，固定开销吃收益&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>我的实际组合：rtk 留给确定性的构建/测试输出（并接受要自己核对退出码），only-cc-lite 内核嵌在自己控制面的代理里，沙箱检索路线只用在搜索结果这种&amp;quot;按需取细节&amp;quot;的输出上。选择标准和压缩率无关，和&amp;quot;这段输出被压错时我能不能发现&amp;quot;有关。另外把所有宣传数字当待验证假设——JetBrains 对 rtk 的复测和 TokenTrust 这类验证层的出现说明，README 表格和真实账单之间有很宽的沟，自己跑一天真实任务比任何对比表可信。&lt;/p></description></item><item><title>2native-ssh-mcp 复盘：一条边界和几个模块设计</title><link>https://daidaij.github.io/p/2native-ssh-mcp-evolution/</link><pubDate>Sat, 12 Sep 2026 08:52:45 +0800</pubDate><guid>https://daidaij.github.io/p/2native-ssh-mcp-evolution/</guid><description>&lt;img src="https://picsum.photos/seed/fdb7c181/800/600" alt="Featured image of post 2native-ssh-mcp 复盘：一条边界和几个模块设计" />&lt;h1 id="2native-ssh-mcp-复盘一条边界和几个模块设计">2native-ssh-mcp 复盘：一条边界和几个模块设计
&lt;/h1>&lt;hr>
&lt;blockquote>
&lt;p>这个 SSH MCP server 8 月底动手，09-11 发到 42 个 commit，Go 实现，单二进制。所有设计决策围绕同一个问题：模型和远端主机之间，边界画在哪。这篇一半讲边界怎么定的，一半讲三个我觉得做得比较有意思的模块——远端进程收割、后台任务协议、输出处理流水线，都带真实代码。&lt;/p>
&lt;/blockquote>
&lt;h2 id="边界凭据和信任材料不经过模型">边界：凭据和信任材料不经过模型
&lt;/h2>&lt;pre class="mermaid" style="visibility:hidden">flowchart LR
A[AI Agent] -- "MCP：4 个工具" --> B[本机进程]
B -- "SSH" --> C[远端主机]
B -.-> D[config.json / ssh-agent]
A -.->|"拿不到"| D&lt;/pre>&lt;p>Agent 只看到 4 个工具（&lt;code>list-servers&lt;/code> / &lt;code>execute-command&lt;/code> / &lt;code>session&lt;/code> / &lt;code>file-transfer&lt;/code>），SSH 连接、密钥、密码都在本机进程里。工具面刻意收敛：后台长任务不是第五个工具，是 &lt;code>execute-command&lt;/code> 的 &lt;code>background: true&lt;/code> 模式；文件传输合成一个 &lt;code>file-transfer&lt;/code>。模型要记的 schema 越少，工具目录占的上下文越少，选错概率也越低。&lt;/p>
&lt;h2 id="模块一远端进程收割">模块一：远端进程收割
&lt;/h2>&lt;p>超时命令的清理是个容易被想简单的问题。杀掉 SSH channel 是没用的——channel 断了，远端命令还在跑，下次连接会看到一堆孤儿进程。而且 in-band 的 &lt;code>Signal&lt;/code> 请求 OpenSSH 经常忽略。我的做法分两步，&lt;code>internal/manager/kill.go&lt;/code>：&lt;/p>
&lt;p>第一步，执行时给命令包一层 pidfile 写入，远端 shell 把自己的 PID 记下来，正常退出时 trap 自动清理：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// The remote shell of an exec channel is its session leader (sshd calls
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// setsid per session), so kill -PID targets the entire group including all
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// children of the command.
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="kd">func&lt;/span> &lt;span class="nf">buildPIDWrapperScript&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">command&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">pidFile&lt;/span> &lt;span class="kt">string&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="kt">string&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nx">fmt&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Sprintf&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s">&amp;#34;echo $$ &amp;gt; %s 2&amp;gt;/dev/null; trap &amp;#39;rm -f %s&amp;#39; EXIT 2&amp;gt;/dev/null; %s&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nf">shellQuote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">pidFile&lt;/span>&lt;span class="p">),&lt;/span> &lt;span class="nf">shellQuote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">pidFile&lt;/span>&lt;span class="p">),&lt;/span> &lt;span class="nx">command&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>第二步，超时（或输出超限中止）时，开一条 secondary exec channel 发收割脚本——不依赖那条可能已经僵死的命令通道：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="kd">func&lt;/span> &lt;span class="nf">remoteKillScript&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">signal&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">pidFile&lt;/span> &lt;span class="kt">string&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="kt">string&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nx">fmt&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nf">Sprintf&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s">&amp;#34;pid=$(cat %s 2&amp;gt;/dev/null); [ -n \&amp;#34;$pid\&amp;#34; ] &amp;amp;&amp;amp; { kill -%s -- -\&amp;#34;$pid\&amp;#34; 2&amp;gt;/dev/null; kill -%s \&amp;#34;$pid\&amp;#34; 2&amp;gt;/dev/null; }; exit 0&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nf">shellQuote&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">pidFile&lt;/span>&lt;span class="p">),&lt;/span> &lt;span class="nx">signal&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">signal&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>两个细节：sshd 每个会话都 setsid，所以远端 shell 就是 session leader，&lt;code>kill -- -PID&lt;/code> 连子进程整组收掉；&lt;code>kill -PID&lt;/code> 后面跟一个裸 PID kill，兜住个别不做 group leader 的 sshd 变体。SIGTERM 之后留 2 秒 grace 再升 SIGKILL，收割脚本本身有硬超时，连接快死也不能把 kill 路径卡住。&lt;/p>
&lt;h2 id="模块二后台任务的三文件协议">模块二：后台任务的三文件协议
&lt;/h2>&lt;p>&lt;code>background: true&lt;/code> 的任务要断连存活、可重附着。实现是一套跑在远端的文件协议，&lt;code>internal/manager/background.go&lt;/code> 生成启动脚本：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt"> 1
&lt;/span>&lt;span class="lnt"> 2
&lt;/span>&lt;span class="lnt"> 3
&lt;/span>&lt;span class="lnt"> 4
&lt;/span>&lt;span class="lnt"> 5
&lt;/span>&lt;span class="lnt"> 6
&lt;/span>&lt;span class="lnt"> 7
&lt;/span>&lt;span class="lnt"> 8
&lt;/span>&lt;span class="lnt"> 9
&lt;/span>&lt;span class="lnt">10
&lt;/span>&lt;span class="lnt">11
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-sh" data-lang="sh">&lt;span class="line">&lt;span class="cl">setsid sh -c &lt;span class="s1">&amp;#39;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="s1"> trap &amp;#34;&amp;#34; HUP
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="s1"> exec &amp;lt;/dev/null
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="s1"> exec &amp;gt;&amp;gt;&amp;#34;$1&amp;#34; 2&amp;gt;&amp;amp;1 # LOG
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="s1"> eval &amp;#34;$2&amp;#34;
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="s1"> echo $? &amp;gt; &amp;#34;$3&amp;#34; # EXITF
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="s1">&amp;#39;&lt;/span> _ &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$LOG&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$body&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$EXITF&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="p">&amp;amp;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nb">echo&lt;/span> &lt;span class="nv">$!&lt;/span> &amp;gt; &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$PIDF&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="c1"># PIDF&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">sleep &lt;span class="m">1&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nv">PID&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="k">$(&lt;/span>cat &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$PIDF&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> 2&amp;gt;/dev/null&lt;span class="k">)&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="o">[&lt;/span> -n &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$PID&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> &lt;span class="o">]&lt;/span> &lt;span class="o">&amp;amp;&amp;amp;&lt;/span> &lt;span class="nb">kill&lt;/span> -0 &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$PID&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span> 2&amp;gt;/dev/null &lt;span class="o">&amp;amp;&amp;amp;&lt;/span> &lt;span class="nb">printf&lt;/span> &lt;span class="s1">&amp;#39;__MCP_BG_STARTED__ pid=%s\n&amp;#39;&lt;/span> &lt;span class="s2">&amp;#34;&lt;/span>&lt;span class="nv">$PID&lt;/span>&lt;span class="s2">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>设计要点：&lt;/p>
&lt;ol>
&lt;li>&lt;code>setsid&lt;/code> + &lt;code>trap &amp;quot;&amp;quot; HUP&lt;/code> + &lt;code>exec &amp;lt;/dev/null&lt;/code> 三件套，SSH 连接断开时 SIGHUP 杀不掉它，真正脱离会话&lt;/li>
&lt;li>LOG / PIDF / EXITF 三个文件就是任务的全部状态：输出追到 LOG，PID 用于后续 stop，退出码写 EXITF。之后随时可以重附着——&lt;code>ReadSessionOutput&lt;/code> 带 offset 读 LOG 增量，退出码从 EXITF 补&lt;/li>
&lt;li>启动握手：&lt;code>sleep 1&lt;/code> 后 &lt;code>kill -0&lt;/code> 验活，活着才回 &lt;code>__MCP_BG_STARTED__ pid=...&lt;/code>，没起来明确报 &lt;code>__MCP_BG_FAILED__&lt;/code>。agent 拿到的是确定性的启动结果，不是&amp;quot;应该启动了&amp;quot;&lt;/li>
&lt;/ol>
&lt;p>对比把任务状态放在服务端内存的方案，这个协议让状态天然持久（文件在远端）、天然可恢复（断线重连照读），服务端重启都不影响任务。&lt;/p>
&lt;h2 id="模块三输出三段流水线">模块三：输出三段流水线
&lt;/h2>&lt;p>所有命令输出在返回给模型前过三段，每段都为&amp;quot;默认开着也不心疼&amp;quot;做过性能设计：&lt;/p>
&lt;p>第一段 ANSI 剥离。转义序列对模型是纯噪声。实现是线性扫描 + 锚点预扫，不是上来就跑全套正则——大部分输出根本没有转义序列，一次扫描就能短路。&lt;/p>
&lt;p>第二段脱敏（&lt;code>redactSecrets&lt;/code> 开启时）。secret 正则扫描很贵（每 MiB 约 200ms），做法是用锚点类别做预门控：任何脱敏 pattern 的匹配必然包含自己类别的锚点词，输出里连锚点都没有就整类跳过；最热的 &lt;code>keyword=value&lt;/code> 类 pattern 再换成一个逐字节等价但快约 30 倍的字节扫描器：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;span class="lnt">8
&lt;/span>&lt;span class="lnt">9
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-go" data-lang="go">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// Anchor classes. Any match of a redaction pattern must contain the class
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// anchor (compared ASCII-case-insensitively — a superset of the case-sensitive
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// PEM pattern), so output that trips no anchor cannot hold a secret.
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="kd">const&lt;/span> &lt;span class="p">(&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">anchorBearer&lt;/span> &lt;span class="p">=&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="o">&amp;lt;&amp;lt;&lt;/span> &lt;span class="kc">iota&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">anchorPEM&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">anchorKV&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">anchorAny&lt;/span> &lt;span class="p">=&lt;/span> &lt;span class="nx">anchorBearer&lt;/span> &lt;span class="p">|&lt;/span> &lt;span class="nx">anchorPEM&lt;/span> &lt;span class="p">|&lt;/span> &lt;span class="nx">anchorKV&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">)&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>第三段大输出落盘（&lt;code>spill.go&lt;/code>）。超过 &lt;code>outputSpillThreshold&lt;/code> 的输出写本地 &lt;code>.ssh-mcp-out/&lt;/code>（目录 0700、文件 0600），工具结果里只留路径、字节数、行数和开头预览，agent 用 Read/Grep 查全文——想看末尾 100 行不用远程重跑命令。落盘目录有 &lt;code>trimSpillDir&lt;/code> 保底只留最新 N 个文件；落盘失败（磁盘问题等）降级为压缩返回，不丢结果。&lt;/p>
&lt;h2 id="边界上的门破坏性命令审批">边界上的门：破坏性命令审批
&lt;/h2>&lt;p>有些命令必须穿过边界。&lt;code>approvalMode: &amp;quot;ask-destructive&amp;quot;&lt;/code> 时，命令命中分类器（内置规则 + &lt;code>approvalPatterns&lt;/code> 扩展 + 豁免名单）就通过 MCP elicitation 弹窗向人确认。两个决策：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>fail-open&lt;/strong>：客户端不支持 elicitation 就照跑，结果里附提示。能力缺口不能把用户锁在门外，锁死比放行代价大&lt;/li>
&lt;li>&lt;strong>弹窗超时 5 分钟&lt;/strong>：声明支持但永远不回答的客户端，不能把工具调用卡死&lt;/li>
&lt;/ol>
&lt;p>时间点上的巧合：做完没多久，MCP 2026-07-28 修订把 elicitation 送进弃用名单（保留 12 个月），替代的 MRTR 语义是返回 &lt;code>input_required&lt;/code> 带上问题、客户端把答案放进 &lt;code>inputResponses&lt;/code> 重试原请求。对审批场景 MRTR 反而更合适：每次审批是可重放、可审计的完整请求；fail-open 的直觉在新语义下退化成&amp;quot;客户端不重试，请求就停在边界上&amp;quot;，错误进不了后端。&lt;/p>
&lt;h2 id="状态放哪会话几乎不装东西">状态放哪：会话几乎不装东西
&lt;/h2>&lt;p>HTTP daemon 模式（常驻 + 多客户端）的代价是并发——加固轮修的 data race、引用计数不收敛、输出预算被并发写穿全是它带来的。daemon 非 loopback 监听强制要求 token，没有直接拒绝启动。&lt;/p>
&lt;p>但 transport 层的 session 几乎不承载状态：SSH 连接在进程内 manager 里，后台任务状态在远端三文件里，重附着靠模型把 session id 当参数传回来。MCP 2026-07-28 把会话从协议里砍掉，对这个工具迁移成本约等于零。&lt;/p>
&lt;h2 id="两个坑">两个坑
&lt;/h2>&lt;ol>
&lt;li>&lt;strong>sftpChunkSize 超过 32KB 是地雷&lt;/strong>（&lt;code>6355520&lt;/code>）。SFTP 协议对包尺寸有 32KB 上限，块大小配置超过会莫名传输失败。默认值钉死，配置范围做钳制&lt;/li>
&lt;li>&lt;strong>传输和命令执行不该共用连接&lt;/strong>。大文件传输长时间占用 SSH 通道，和命令互相干扰，后来把传输挪到独立连接（&lt;code>221cf27&lt;/code> sftpDedicatedConn）&lt;/li>
&lt;/ol>
&lt;h2 id="小结">小结
&lt;/h2>&lt;p>回收成一条检验标准：新功能进这个项目前，先问这个状态或材料放边界的哪一侧——凭据放进程里（模型看不见），任务状态放远端三文件（模型按需读），大输出放本地磁盘（agent 自己 Grep），危险操作交给人（穿边界走门）。凭据隔离是安全上的边界，输出处理是 token 上的边界，两条线共用同一套思路。&lt;/p></description></item><item><title>给 MCP 服务器做减法（二）：从 MCP 退到 CLI 的 fail-loud 改造</title><link>https://daidaij.github.io/p/cbm-mcp-to-cli/</link><pubDate>Sun, 06 Sep 2026 20:05:43 +0800</pubDate><guid>https://daidaij.github.io/p/cbm-mcp-to-cli/</guid><description>&lt;img src="https://picsum.photos/seed/a7855219/800/600" alt="Featured image of post 给 MCP 服务器做减法（二）：从 MCP 退到 CLI 的 fail-loud 改造" />&lt;h1 id="给-mcp-服务器做减法二从-mcp-退到-cli-的-fail-loud-改造">给 MCP 服务器做减法（二）：从 MCP 退到 CLI 的 fail-loud 改造
&lt;/h1>&lt;hr>
&lt;blockquote>
&lt;p>上一篇讲的是 token 账——cbm 和 codegraph 两件套的定义层税有 ~8,600 tokens/会话。这一篇讲更彻底的一刀：把 cbm（codebase-memory-mcp）整个从 MCP 进程形态退到 CLI 形态，以及退的过程中撞出来的 fail-loud 教训。数据都是本机实测（Windows 11，cbm v0.10.8，codegraph v1.6.0）。&lt;/p>
&lt;/blockquote>
&lt;h2 id="mcp-形态的固定成本不只是-token">MCP 形态的固定成本：不只是 token
&lt;/h2>&lt;hr>
&lt;p>先看进程结构。MCP 形态下，cbm 是三层：&lt;/p>
&lt;pre class="mermaid" style="visibility:hidden">flowchart LR
A[MCP 客户端] -- stdio, 每会话一个 --> B[stdio server]
B -- IPC --> C[中心 daemon]
C -- fork --> D[index worker 池]&lt;/pre>&lt;p>这个结构意味着三笔固定成本，而且没有一笔能省掉：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>每会话拉起一个 stdio server&lt;/strong>。只要有 N 个客户端连着，就有 N 份进程开销。上游 #1764 报的就是这个：空闲 MCP 客户端每个烧 ~0.7 核，N 会话 = N 核。&lt;/li>
&lt;li>&lt;strong>daemon 冷启&lt;/strong>。CLI 侧实测冷启一次 daemon 约 5 秒；MCP 侧更糟，上游 #1955 报新客户端要等 15-30s 才能连上健康 daemon。&lt;/li>
&lt;li>&lt;strong>worker 的资源上限形同虚设&lt;/strong>。我实测索引本仓库（~1900 文件的 C 项目）时 worker 吃到 &lt;strong>2.7GB 内存&lt;/strong>（预算 2.8GB 几乎打满）、242s CPU；282MB 的单个 exe；stdio server 在空闲时段还平均烧 ~30% 单核。预算是 advisory 的（#1973），宁可 OOM 也不降级（#1997 报 14k 文件 monorepo 全量索引分配 ~11.5GB，无视配置）。&lt;/li>
&lt;/ol>
&lt;p>然后才是 token 账。cbm 的 MCP 定义层（tools/list + initialize.instructions + prompts）实测 ~5,850 tok，15 个工具 22.4KB；跟 codegraph 合计 ~8,600 tok/会话。而且 cbm 的结果还是&lt;strong>双份&lt;/strong>返回——tree 文本 + structuredContent JSON，同一份数据塞两遍进上下文。&lt;/p>
&lt;blockquote>
&lt;p>单看哪个数字都不致命，叠起来就是&amp;quot;我明明没怎么用它，它却一直在烧&amp;quot;的体感。最后那根稻草是 watcher，先看 2026-09-04 那次实测的完整账单：&lt;/p>
&lt;/blockquote>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>指标&lt;/th>
&lt;th>实测值&lt;/th>
&lt;th>备注&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>index worker 内存&lt;/td>
&lt;td>&lt;strong>2.7GB&lt;/strong>（预算 2.8GB 几乎打满）&lt;/td>
&lt;td>PID 1172，索引本仓库&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>index worker CPU&lt;/td>
&lt;td>242s / 运行约 2 分钟&lt;/td>
&lt;td>~21% 单核&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>MCP stdio server CPU&lt;/td>
&lt;td>330+ CPU 秒 / 17 分钟&lt;/td>
&lt;td>空闲时段平均 ~30% 单核&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>二进制体积&lt;/td>
&lt;td>&lt;strong>282MB&lt;/strong> 单个 exe&lt;/td>
&lt;td>cbm.zip 才 37MB&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>watcher 行为&lt;/td>
&lt;td>&lt;code>strategy=git&lt;/code> 每 3~5 秒一轮 + 每轮 reap 一个 worker&lt;/td>
&lt;td>自触发重索引循环，#1953&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>同仓库 codegraph 对照&lt;/td>
&lt;td>磁盘 1.1GB（db 160MB + &lt;strong>WAL 882MB&lt;/strong>）&lt;/td>
&lt;td>checkpoint 饥饿，与 cbm #1083 同款病&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>worker 触发链当时是实锤的：会话工具在仓库内写 &lt;code>.codegraph/&lt;/code>、&lt;code>graphify-out/&lt;/code>（未 gitignore、untracked）→ git-strategy watcher 判定 changed → daemon 反复 fork worker。这不是低概率 bug，是&lt;strong>默认配置的必然后果&lt;/strong>——watcher 默认开、git 策略对 untracked 敏感、而工具产物恰恰就是 untracked 文件。上游四连发可以佐证这不是孤例：#1953 watcher 自触发、#2015 worker 失败无退避（有人 4h43m 里 fork 了 2233 个 worker）、#1083 WAL 无上限（有人跑出 115GB）、#1991 每请求 ~12ms 固定地板。&lt;/p>
&lt;h2 id="为什么裁剪在-mcp-侧做不干净">为什么裁剪在 MCP 侧做不干净
&lt;/h2>&lt;hr>
&lt;p>第一反应是裁工具面。cbm 支持 &lt;code>--tool-profile=scout&lt;/code>，实测能从 argv 解析、立即生效，定义层 ~8.6K → ~5.8K tok。但往下走就发现问题了：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">档位 1 scout 配置 → 省 32%，零改动，但 15 个工具里只留一小撮
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">档位 2 自编译加 arch 档 → 要改 src/mcp/mcp.c 的 mcp_tool_allowed()，约 20 行
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">档位 3 退出 MCP 走 CLI → 省 68%，15 个工具全部可用
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>档位 2 的尴尬在于：我想要的&amp;quot;arch 档&amp;quot;（get_architecture、query_graph、detect_changes、index_status、check_index_coverage、list_projects 这 6 个 codegraph 没有的工具）上游没有，得自己打补丁。而 cbm 0.10.x 几乎周更，上游自己的 profile 裁剪 PR（#1649 思路的 DACL 补丁）已经在冲突边缘——&lt;strong>大补丁 rebase 会痛，这是结构性问题，不是手艺问题&lt;/strong>。&lt;/p>
&lt;p>更隐蔽的是&lt;strong>配置漂移&lt;/strong>。MCP 形态下 cbm 的行为散落在好几处：&lt;code>_config.db&lt;/code>（藏在 cache 目录里，#1744 报升级会丢设置）、config.json、argv、daemon 启动时机。两个具体的坑：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">watcher_enabled → 只在 daemon 启动时读，改完必须 daemon stop 再起
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">daemon 环境变量 → 被首个会话固化，后续会话无法按需调整
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>也就是说，就算工具面裁干净了，&lt;strong>运行时行为还是被&amp;quot;谁先启动谁说了算&amp;quot;的 daemon 固化&lt;/strong>——每会话一个 stdio server + 中心 daemon 的三层结构里，daemon 环境被第一个连上来的会话定型，后面的人只能接受。&lt;/p>
&lt;blockquote>
&lt;p>这是我最终选档位 3 的真正原因：不是 token 省 68% 有多诱人，而是 MCP 形态下&amp;quot;裁剪&amp;quot;永远是在别人的进程模型里打补丁。CLI 形态下，进程模型是我的，每次调用都是干净的一次性子进程，没有固化、没有漂移。&lt;/p>
&lt;/blockquote>
&lt;h2 id="退到-cli全量-15-个工具选择权交给-skill">退到 CLI：全量 15 个工具，选择权交给 skill
&lt;/h2>&lt;hr>
&lt;p>形态变化一句话：cbm 不再注册为 MCP server，改用&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">codebase-memory-mcp cli --json &amp;lt;tool&amp;gt; &lt;span class="o">[&lt;/span>args...&lt;span class="o">]&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>按需子进程调用。它有个很方便的性质：&lt;strong>每个 MCP 工具都有 CLI 等价物&lt;/strong>，所以&amp;quot;裁剪&amp;quot;这个概念直接消失了——15 个工具全量可调，一个不少，也就不存在&amp;quot;profile 裁不干净&amp;quot;的问题。工具面不再占据每个会话的上下文，调用时才产生成本。&lt;/p>
&lt;p>定义层的账当场就变了：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>组合（定义层）&lt;/th>
&lt;th>≈ tokens/会话&lt;/th>
&lt;th>相对两件套&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>cbm MCP + codegraph MCP（现状）&lt;/td>
&lt;td>~8,600&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>cbm scout 档 + codegraph&lt;/td>
&lt;td>~5,840&lt;/td>
&lt;td>省 32%&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>cbm arch 补丁档 + codegraph&lt;/td>
&lt;td>~4,550&lt;/td>
&lt;td>省 47%&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>cbm 退 CLI，仅 codegraph 进 MCP&lt;/strong>&lt;/td>
&lt;td>&lt;strong>~2,750&lt;/strong>&lt;/td>
&lt;td>&lt;strong>省 68%&lt;/strong>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>而且 bytes/4 是乐观估算——schema 是标点密集的 JSON，真实分词约 bytes/3~3.5，两件套体感可能到 ~10-11K tokens。CLI 形态下这笔税直接归零。&lt;/p>
&lt;p>代价是模型看不到工具列表了，&amp;ldquo;什么时候用哪个命令&amp;quot;得有人管。我的方案是用 skill 规约约束，而且刻意收得很窄——只保留三个实测有效的场景，其余一律禁用：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>场景&lt;/th>
&lt;th>命令&lt;/th>
&lt;th>为什么留给 cbm&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>架构概览&lt;/td>
&lt;td>&lt;code>cbm cli get_architecture&lt;/code>（aspects=clusters/cycles/hotspots）&lt;/td>
&lt;td>cbm 独有，codegraph 无架构视图&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>图查询&lt;/td>
&lt;td>&lt;code>cbm cli query_graph&lt;/code>（Cypher、复杂度门槛）&lt;/td>
&lt;td>cbm 独有&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>commit 影响半径&lt;/td>
&lt;td>&lt;code>cbm cli detect_changes&lt;/code>（git diff → 传递影响集）&lt;/td>
&lt;td>diff 基，codegraph 的 impact 是符号基&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>规约里还给三类典型工作流定了调用法：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt"> 1
&lt;/span>&lt;span class="lnt"> 2
&lt;/span>&lt;span class="lnt"> 3
&lt;/span>&lt;span class="lnt"> 4
&lt;/span>&lt;span class="lnt"> 5
&lt;/span>&lt;span class="lnt"> 6
&lt;/span>&lt;span class="lnt"> 7
&lt;/span>&lt;span class="lnt"> 8
&lt;/span>&lt;span class="lnt"> 9
&lt;/span>&lt;span class="lnt">10
&lt;/span>&lt;span class="lnt">11
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">开源调研 clone 后 codegraph init + index（一次全量，之后增量自动）；
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> cbm 只在确定要做架构级分析时 index_repository mode=full（语义边只有 full/moderate 有），
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> 用完 daemon stop；大仓（&amp;gt;1 万文件）用 mode=fast 先垫底。
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> 引用代码前必跑 check_index_coverage——parse_partial 区域的图结果不可信。
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">二次开发 日常 impact/callers 走 codegraph；提交前跑一次 cbm detect_changes 当&amp;#34;影响面自检&amp;#34;，
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> 配合 query_graph 查改动的传递调用深度。
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> cbm watcher 关掉（#1953），索引新鲜度靠提交前手动 index。
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">自研项目 仓库 &amp;lt; 几千文件时 codegraph 一个就够；cbm 只做里程碑体检
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> （get_architecture aspects=cycles,hotspots 查循环依赖和上帝节点）。
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>配套的索引分层原则也写进了规约：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">codegraph = 活层（always-on，保存 ~0.3s 增量，喂日常符号工作）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">cbm = 深层（milestone / 按需全量，喂架构分析与影响面，用完 daemon stop）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>会话开始时 hook 自动 init/sync，我不手动管索引；cbm 只在确定要做架构级分析时 &lt;code>index_repository&lt;/code> 一次，用完 &lt;code>daemon stop&lt;/code>。产物目录（&lt;code>.codegraph/&lt;/code>、&lt;code>.codebase-memory/&lt;/code>）一律 gitignore，防 watcher 互喂。&lt;/p>
&lt;blockquote>
&lt;p>这里有个反直觉的取舍：CLI 化之后单次冷启 daemon 要 ~5 秒，比 MCP 常驻&amp;quot;慢&amp;quot;了。但对一个每会话只按需调两三次的深层工具，5 秒 × 2 次 &amp;lt; 常驻进程烧掉的一切——包括那个 watcher 死循环和 2.7GB worker。低频工具的合理形态就是一次性子进程，不是常驻服务。&lt;/p>
&lt;/blockquote>
&lt;h2 id="fail-loud静默空结果比报错更危险">fail-loud：静默空结果比报错更危险
&lt;/h2>&lt;hr>
&lt;p>CLI 化过程中最值钱的教训是这个：&lt;strong>工具返回&amp;quot;空结果&amp;quot;有两种可能——真的没有，或者工具坏了&lt;/strong>。MCP 形态下这两者都表现为一个正常的响应，模型根本分不清。三个实锤案例（记载在我的环境备忘里，均为实测）：&lt;/p>
&lt;p>&lt;strong>案例一：upstream #1682。&lt;/strong> CALLS 边丢失时，&lt;code>trace_path&lt;/code> 返回 &lt;code>callers_total: 0&lt;/code>——和&amp;quot;这个函数真的没有调用方&amp;quot;在响应格式上&lt;strong>完全不可区分&lt;/strong>。我一开始信了图谱的&amp;quot;没有调用方&amp;rdquo;，后来 grep 复核才发现调用方明明存在。&lt;/p>
&lt;p>&lt;strong>案例二：&lt;code>detect_changes&lt;/code> 对非法 direction 静默返回空。&lt;/strong> 传错参数不报错，返回一个空的影响集。影响面自检场景里，&amp;ldquo;空&amp;quot;意味着&amp;quot;这次改动无风险&amp;rdquo;——把工具错误翻译成了错误的安全结论。&lt;/p>
&lt;p>&lt;strong>案例三：&lt;code>since&lt;/code> 参数拒收 &lt;code>^&lt;/code> 后缀。&lt;/strong> 传 &lt;code>v1.0^&lt;/code> 直接被拒。这算好的，好歹是个显式失败——但如果不看退出码，管道里它同样表现为&amp;quot;空&amp;quot;。&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">危险的共同模式：
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> 输入非法 / 内部状态损坏
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> → 静默返回空集
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> → 模型把&amp;#34;空&amp;#34;读成&amp;#34;事实上的不存在&amp;#34;
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> → 下游结论全错，且无任何报错痕迹
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>防御手段我总结成三条，全部写进了 skill 规约：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>否定性结论必须 grep 复核&lt;/strong>。&amp;ldquo;没有调用方/没有引用&amp;quot;这类结论，图谱零结果不可信——静默漏报与真实无调用不可区分，先 grep 原文再下结论。&lt;/li>
&lt;li>&lt;strong>批量修改必须带断言&lt;/strong>。用脚本补丁改配置时 &lt;code>assert count == 1&lt;/code>，失败即整体回滚。我实际靠这个救回过两次——python 补丁锚点缩进错误，把一批配置键插进了错误的嵌套层级（比如插到 &lt;code>themeVariables.xyChart&lt;/code> 内部），键&amp;quot;存在&amp;quot;但静默失效。批量改配置结构后还要用括号深度计数脚本断言键的层级，或者干脆整段重写而不是锚点替换。&lt;/li>
&lt;li>&lt;strong>黑名单 + 全量审计代替打地鼠&lt;/strong>。检测的&amp;quot;坏值清单&amp;quot;必须有定义来源（比如主题调色板全集），只列已知坏值会漏掉&amp;quot;自家配置被误用&amp;quot;这类新形态。正确姿势是先渲染全部形态 → 像素级/结构级普查 → 对照定义来源全集，一次扫完，而不是发现一个修一个。&lt;/li>
&lt;/ol>
&lt;p>把这三条合在一起，就是 skill 规约里给所有图谱类工具定的防呆前置：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">调图谱工具前： index_status 确认索引新鲜度（索引落后时 detect_changes 结果失真）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">调完拿结论前： 结论是&amp;#34;空/没有/零&amp;#34; → 强制 grep 复核，禁止直接采信
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">写脚本改配置： assert count==1 + 层级断言，失败即整体回滚
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;blockquote>
&lt;p>这三条没有一条是 cbm 特有的——任何&amp;quot;返回结构化结果&amp;quot;的工具都适用。图谱只是把问题放大了，因为它看起来太可信了：返回带行号、带符号名的精确结果，模型（和我）都倾向于直接信。&lt;/p>
&lt;/blockquote>
&lt;p>有意思的是，这个原则直接催生了 fork 补丁计划里的 #4：给 cbm 加&lt;strong>配置化工具禁用清单&lt;/strong>（tools_disabled，双侧生效），要求禁用必须 fail-loud——调用被禁的工具要显式报错而不是静默失败，且 &lt;code>--help&lt;/code> 输出与实际工具面一致。禁用一个工具后它还能被调用、或者 help 里还列着，都是配置漂移的静默形态。&lt;/p>
&lt;blockquote>
&lt;p>fail-loud 说白了就一句话：宁可让错误炸在这一层，也不要让它变成一层之下的&amp;quot;空结果&amp;rdquo;。静默空结果最阴险的地方在于它&lt;strong>消耗的是信任而不是报错&lt;/strong>——你不会去排查一个&amp;quot;正常&amp;quot;的空响应。&lt;/p>
&lt;/blockquote>
&lt;h2 id="windows-的-dacl-仪式">Windows 的 DACL 仪式
&lt;/h2>&lt;hr>
&lt;p>CLI 化没解决的一个问题：cbm 在 Windows 上对 daemon endpoint 做强制 ACL 检查（上游 #1624/#1856/#2023 的 ACL 设计），撞上时直接报 &lt;code>secure daemon endpoint could not be created&lt;/code>。&lt;/p>
&lt;p>我踩过的绕法，按代价排序：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># 1. 搬运行时目录（官方思路，但只能搬家不能豁免检查本身）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nv">CBM_RUNTIME_DIR&lt;/span>&lt;span class="o">=&lt;/span>D:&lt;span class="se">\d&lt;/span>ata&lt;span class="se">\c&lt;/span>odebase-memory-mcp&lt;span class="se">\r&lt;/span>untime
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># 2. 临时绕过：收紧目标目录 ACL 让检查通过（D:\data 这么干过，事后要 icacls /reset 恢复）&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># 3. fork 补丁计划 #2：加 CBM_SKIP_DACL_HARDENING 开关&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>#2 的关键是：CLI 形态&lt;strong>同样&lt;/strong>受 DACL 检查拖累——退到 CLI 躲开了 MCP 的进程结构税，但躲不开单机安全检查对本地回环通信的开销。&lt;code>CBM_RUNTIME_DIR&lt;/code> 只是把战场挪走，检查本身还在跑。所以补丁计划里专门立了 #2：一个显式的跳过开关，本地单人场景没必要每次都做安全仪式。&lt;/p>
&lt;blockquote>
&lt;p>这类问题在 Linux 上根本不存在，纯 Windows 单机税。但代价结构很典型：&lt;strong>设计给多用户/低信任场景的防御，在单用户高信任环境里变成每个请求都要交的过路费&lt;/strong>。和 MCP 工具面裁剪是同构的——都是&amp;quot;默认配置为我不存在的场景付费&amp;quot;。&lt;/p>
&lt;/blockquote>
&lt;h2 id="fork-补丁计划把教训变成-issue">fork 补丁计划：把教训变成 issue
&lt;/h2>&lt;hr>
&lt;p>最后整理一下 fork 仓库的 issue 清单。写出来是因为每一条背后都是上面某一节的一个教训：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>#&lt;/th>
&lt;th>内容&lt;/th>
&lt;th>状态&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>#1&lt;/td>
&lt;td>CLI 方案取代 profile 裁剪&lt;/td>
&lt;td>&lt;strong>已关闭&lt;/strong>——直接退 CLI，profile 档位没了存在必要&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>#2&lt;/td>
&lt;td>&lt;code>CBM_SKIP_DACL_HARDENING&lt;/code> 开关&lt;/td>
&lt;td>待做——CLI 侧同样受检查拖累&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>#3&lt;/td>
&lt;td>默认值反转&lt;/td>
&lt;td>待做——已补 CLI 侧证据：daemon 冷启 ~5s/次、detect_changes 非法 direction 静默空、since 拒收 &lt;code>^&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>#4&lt;/td>
&lt;td>配置化工具禁用清单（tools_disabled 双侧生效 + fail-loud + &lt;code>--help&lt;/code> 一致性）&lt;/td>
&lt;td>待做——挡住实测判负的 5 个工具被误用&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>补丁纪律沿用之前定下的：&lt;strong>机械、小、可重放&lt;/strong>。上游几乎周更，#1649 已经在冲突边缘，任何超出&amp;quot;加一组名字/加一个开关&amp;quot;粒度的补丁都是给未来的自己挖坑。&lt;/p>
&lt;p>值得单独说的是 #1 被关闭这个信号：最初计划里&amp;quot;给 MCP 加更好的 profile 档位&amp;quot;是正经理方案，做着做着发现 CLI 形态让整个问题消失了。&lt;strong>最好的裁剪是不需要裁剪&lt;/strong>——工具面从来不进上下文，就无所谓留哪 6 个砍哪 9 个。&lt;/p>
&lt;h2 id="这套退法在整体方案里的位置">这套退法在整体方案里的位置
&lt;/h2>&lt;hr>
&lt;p>退 CLI 不是孤立动作，它是一个分层方案的地基。完整规划是三步：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">P0 只包 codegraph 进 MCP（4 个短工具：search / source / relations / impact）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">P1 CBM 保持 CLI 按需调用——本文讲的退法就是这一步的落地
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">P2 仅当&amp;#34;出会话跑 CLI&amp;#34;摩擦大到不可接受，才在门面里延迟拉起 CBM 三个工具
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> （第一次调用才 spawn cbm cli，空闲超时 daemon stop）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>配套的验收清单里，几条硬指标直接针对 MCP 形态的老病：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-text" data-lang="text">&lt;span class="line">&lt;span class="cl">- 会话 tools/list 工具数 ≤ 8，描述合计 ≤ 4KB（对 ~22.4KB 的 cbm 全量）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">- 日常 search/source/relations/impact 不启动 cbm.exe / cbm daemon
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">- 结果无双份 JSON（cbm 的 tree + structuredContent 双份是重点封杀对象）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">- 需要架构分析时有文档化的 CLI 一步（含 daemon stop）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>同样重要的是&amp;quot;明确不做&amp;quot;清单：不给 cbm 关 LSP、拆 daemon、硬内存上限——那是敌对 fork，和周更上游打架。改不动的地方绕过去（CLI、CBM_RUNTIME_DIR），绕不过去的立 issue（#2/#3/#4），这就是补丁计划全部是&amp;quot;开关级&amp;quot;改动的原因。&lt;/p>
&lt;hr>
&lt;blockquote>
&lt;p>复盘下来，&amp;ldquo;从 MCP 退到 CLI&amp;quot;与其说是性能优化，不如说是一次责任边界的回收：进程模型、调用时机、失败语义，全部收回到我自己的 skill 规约里。代价是我要自己维护规约、自己复核否定性结论。但考虑到静默空结果曾经让我把&amp;quot;图谱说没有调用方&amp;quot;当成事实写进结论——这笔交易不亏。&lt;/p>
&lt;/blockquote></description></item><item><title>给 MCP 服务器做减法（一）：实测判负的工具与裁剪依据</title><link>https://daidaij.github.io/p/cbm-toolset-pruning/</link><pubDate>Sun, 06 Sep 2026 20:04:24 +0800</pubDate><guid>https://daidaij.github.io/p/cbm-toolset-pruning/</guid><description>&lt;img src="https://picsum.photos/seed/e7f46986/800/600" alt="Featured image of post 给 MCP 服务器做减法（一）：实测判负的工具与裁剪依据" />&lt;h1 id="给-mcp-服务器做减法一实测判负的工具与裁剪依据">给 MCP 服务器做减法（一）：实测判负的工具与裁剪依据
&lt;/h1>&lt;hr>
&lt;blockquote>
&lt;p>我深度用了两周 codebase-memory-mcp（下称 cbm），一个把代码库索引成知识图谱的 MCP 服务器，15 个工具。结论是：15 个里只有 3 个场景真正立住了，其余全部判负——要么被 codegraph 同类工具碾压，要么 CLI 里一行命令就有等价物，不值得在每轮对话里交定义层税。这篇记录判负的证据和方法，不记录情绪。&lt;/p>
&lt;/blockquote>
&lt;h2 id="先算账15-个工具的定义层税单">先算账：15 个工具的定义层税单
&lt;/h2>&lt;hr>
&lt;blockquote>
&lt;p>判负之前先量化&amp;quot;养着这些工具要花多少钱&amp;quot;。MCP 的成本模型里有个容易被忽略的项：工具定义不是调用时才付费，而是&lt;strong>每个会话每轮对话都常驻在上下文里&lt;/strong>。&lt;/p>
&lt;/blockquote>
&lt;p>cbm 是 stdio server，我就以 MCP 客户端身份把它拉起来，抓 &lt;code>tools/list&lt;/code> 的真实载荷。探针是个几十行的 Node 脚本（简化版）：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt"> 1
&lt;/span>&lt;span class="lnt"> 2
&lt;/span>&lt;span class="lnt"> 3
&lt;/span>&lt;span class="lnt"> 4
&lt;/span>&lt;span class="lnt"> 5
&lt;/span>&lt;span class="lnt"> 6
&lt;/span>&lt;span class="lnt"> 7
&lt;/span>&lt;span class="lnt"> 8
&lt;/span>&lt;span class="lnt"> 9
&lt;/span>&lt;span class="lnt">10
&lt;/span>&lt;span class="lnt">11
&lt;/span>&lt;span class="lnt">12
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-js" data-lang="js">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// mcp_probe.cjs：拉起 stdio server，抓 initialize + tools/list 全量载荷
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="kr">const&lt;/span> &lt;span class="nx">child&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="nx">spawn&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nx">cmd&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">args&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="p">{&lt;/span> &lt;span class="nx">stdio&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="p">[&lt;/span>&lt;span class="s2">&amp;#34;pipe&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s2">&amp;#34;pipe&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s2">&amp;#34;inherit&amp;#34;&lt;/span>&lt;span class="p">]&lt;/span> &lt;span class="p">});&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nx">send&lt;/span>&lt;span class="p">({&lt;/span> &lt;span class="nx">jsonrpc&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;2.0&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">id&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">method&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;initialize&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nx">params&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="p">{&lt;/span> &lt;span class="nx">protocolVersion&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;2024-11-05&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">capabilities&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="p">{},&lt;/span> &lt;span class="nx">clientInfo&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="p">{&lt;/span> &lt;span class="nx">name&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;probe&amp;#34;&lt;/span> &lt;span class="p">}&lt;/span> &lt;span class="p">}&lt;/span> &lt;span class="p">});&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nx">send&lt;/span>&lt;span class="p">({&lt;/span> &lt;span class="nx">jsonrpc&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;2.0&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">method&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;notifications/initialized&amp;#34;&lt;/span> &lt;span class="p">});&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nx">send&lt;/span>&lt;span class="p">({&lt;/span> &lt;span class="nx">jsonrpc&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;2.0&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">id&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="mi">2&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">method&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="s2">&amp;#34;tools/list&amp;#34;&lt;/span> &lt;span class="p">});&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="nx">child&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">stdout&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">on&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s2">&amp;#34;data&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">buf&lt;/span> &lt;span class="p">=&amp;gt;&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="c1">// 按 Content-Length 拆帧，落盘每个响应的原始字节数
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span> &lt;span class="nx">frames&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">push&lt;/span>&lt;span class="p">({&lt;/span> &lt;span class="nx">id&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="nx">msg&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">id&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">bytes&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="nx">buf&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="nx">length&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nx">payload&lt;/span>&lt;span class="o">:&lt;/span> &lt;span class="nx">msg&lt;/span> &lt;span class="p">});&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">});&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>初版只量了 tools/list，后来发现漏了两项：MCP initialize 响应里的 &lt;code>instructions&lt;/code> 字段（规范客户端会注入系统提示词）和 prompts 列表。补齐后的完整账单：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>定义层&lt;/th>
&lt;th>codegraph&lt;/th>
&lt;th>cbm 全量&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>tools/list（描述+schema）&lt;/td>
&lt;td>6,762B ≈ 1,691 tok&lt;/td>
&lt;td>22,441B ≈ 5,610 tok（&lt;strong>15 个工具&lt;/strong>）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>instructions 字段&lt;/td>
&lt;td>4,226B ≈ 1,057 tok&lt;/td>
&lt;td>808B ≈ 202 tok&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>prompts 列表&lt;/td>
&lt;td>无&lt;/td>
&lt;td>2 个 ≈ 37 tok&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>定义层小计&lt;/strong>&lt;/td>
&lt;td>&lt;strong>≈ 2,748 tok&lt;/strong>&lt;/td>
&lt;td>&lt;strong>≈ 5,850 tok&lt;/strong>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>payload 字节数到手后换算 token。这里有个容易自欺的地方：用 bytes/4 估是乐观值，对 schema 这种标点密集的 JSON，真实分词更接近 bytes/3~3.5：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-js" data-lang="js">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// 估算口径：两档一起报，给区间不给单点
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="kr">const&lt;/span> &lt;span class="nx">tokensOptimistic&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="nx">bytes&lt;/span> &lt;span class="o">/&lt;/span> &lt;span class="mi">4&lt;/span>&lt;span class="p">;&lt;/span> &lt;span class="c1">// 乐观下界
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="kr">const&lt;/span> &lt;span class="nx">tokensRealistic&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="nx">bytes&lt;/span> &lt;span class="o">/&lt;/span> &lt;span class="mf">3.5&lt;/span>&lt;span class="p">;&lt;/span> &lt;span class="c1">// 标点密集 JSON 的经验档
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>两个 server 一起挂着，定义层合计 &lt;strong>≈ 8,600 tokens/会话&lt;/strong>，乐观估算下真实体感可能到 10-11K。&lt;/p>
&lt;blockquote>
&lt;p>最扎眼的是 codegraph 那行 4.2KB 的 instructions：一整套行为指令（&amp;ldquo;直接回答别委派子 agent&amp;quot;&amp;ldquo;先于 Read/Grep 调用&amp;rdquo;）。这是把工具用法写死进系统提示词，替模型做工作方式决策。cbm 反过来，instructions 很短，但用 15 个工具的长描述把复杂性全堆在 tools/list 里。两种路线，交的税一样。&lt;/p>
&lt;/blockquote>
&lt;p>调用时的开销另算，而且更隐蔽：cbm 每个结果是&lt;strong>双份返回&lt;/strong>——tree 文本和 structuredContent JSON 是同一份数据给两遍，结果侧直接 ×2。codegraph 单份但载荷密，官方自己承认常驻上下文 +80%。&lt;/p>
&lt;p>结果侧的浪费也顺手写进了探针：同一个结果，&lt;code>content&lt;/code> 里是 tree 文本，&lt;code>structuredContent&lt;/code> 里是同数据的 JSON 序列化。对模型来说信息为零增益，token 直接翻倍。&lt;/p>
&lt;h2 id="判负的标准三条硬证据">判负的标准：三条硬证据
&lt;/h2>&lt;hr>
&lt;blockquote>
&lt;p>&amp;ldquo;我感觉没用&amp;quot;不是裁剪依据，MCP 工具面的每一次删减都要能回答：删了之后工作流会不会退化？&lt;/p>
&lt;/blockquote>
&lt;p>我的判负标准是三条，全部可复测：&lt;/p>
&lt;ol>
&lt;li>&lt;strong>真实调用率&lt;/strong>：回看若干个真实会话的调用记录，哪些工具被 agent 主动调过；&lt;/li>
&lt;li>&lt;strong>结果质量&lt;/strong>：调了之后，是否真的省掉了后续的 grep/Read 次数，还是拿到结果还得回原始文件验证；&lt;/li>
&lt;li>&lt;strong>CLI 等价性&lt;/strong>：这个工具在 &lt;code>cbm cli&lt;/code> 里有没有一行等价命令——有的话，退出 MCP 只损失&amp;quot;agent 自主发现&amp;quot;这一点，能力不丢。&lt;/li>
&lt;/ol>
&lt;p>第 1 条有个陷阱要说明：agent 不调 ≠ 工具差。可能是 schema 写得糊、描述太长模型没读进去。但对裁剪来说结论一样成立——&lt;strong>一个 agent 不会主动调的工具，挂在工具面里就是纯税&lt;/strong>。&lt;/p>
&lt;blockquote>
&lt;p>这三条里第 3 条最关键，它把&amp;quot;裁剪&amp;quot;从减法变成了搬家：MCP 定义层省下的 token 是确定的，能力通过 CLI 随时可取回。没有等价物的工具才需要真正纠结去留。&lt;/p>
&lt;/blockquote>
&lt;p>还有一个反向的佐证：实测里 agent 对工具的&lt;strong>误用&lt;/strong>集中出现在少数几个工具上——schema 越长、描述越糊，模型越容易拿错误的参数组合去撞。工具面越长这个问题越严重，15 个工具互相干扰，单个工具的描述再精准也救不回来。&lt;/p>
&lt;h2 id="逐个判负砍掉的留下的可惜的">逐个判负：砍掉的、留下的、可惜的
&lt;/h2>&lt;hr>
&lt;blockquote>
&lt;p>15 个工具过一遍筛子。判负名单按&amp;quot;被谁替代&amp;quot;分组，证据都来自实测记录和上游 issue。&lt;/p>
&lt;/blockquote>
&lt;pre class="mermaid" style="visibility:hidden">flowchart LR
A[cbm 15 个工具] --> B{三条硬证据筛}
B -->|被 codegraph 碾压| C[判负组 1：符号级查询]
B -->|CLI 有等价物| D[判负组 2：管理面]
B -->|真实会话零调用| E[判负组 3：list_projects]
B -->|三场景对照实验立住| F[保留：3 个场景工具]&lt;/pre>&lt;p>&lt;strong>判负组 1：符号级查询，被 codegraph 碾压。&lt;/strong> 这是最大的一组。符号搜索、取源码、调用链——这些是 codegraph 的基本盘，而且强得多：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>能力&lt;/th>
&lt;th>cbm 工具&lt;/th>
&lt;th>codegraph 对应&lt;/th>
&lt;th>判负理由&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>符号搜索&lt;/td>
&lt;td>search_graph&lt;/td>
&lt;td>search&lt;/td>
&lt;td>codegraph 一次返回限定元数据，够用且轻&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>取源码&lt;/td>
&lt;td>get_code_snippet&lt;/td>
&lt;td>node / &lt;strong>explore&lt;/strong>&lt;/td>
&lt;td>explore 一次拉一组符号的完整源码，cbm 做不到&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>调用链&lt;/td>
&lt;td>trace_path&lt;/td>
&lt;td>callers / callees / trace&lt;/td>
&lt;td>cbm 有 #1991：每请求固定 ~12ms 地板（0.9.0 时代是亚毫秒），codegraph 无此税&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;code>trace_path&lt;/code> 的判负最干脆：调用链分析 codegraph 的 &lt;code>callers&lt;/code>/&lt;code>callees&lt;/code>/&lt;code>trace&lt;/code> 全覆盖，cbm 这边还背着 12ms/请求的进程间协调地板。同一次查询，没有理由选慢的那个。&lt;/p>
&lt;p>&lt;strong>判负组 2：管理面，全走 CLI。&lt;/strong> 索引、同步、daemon 控制（&lt;code>index_repository&lt;/code>、&lt;code>daemon stop&lt;/code>、config 类）占了好几个工具位，但它们的调用者是&lt;strong>人&lt;/strong>，不是 agent——我从来没需要让模型自主决定&amp;quot;什么时候重建索引&amp;rdquo;。这类工具留在 MCP 工具面里，等于让每轮对话为低频管理操作付常驻税。&lt;/p>
&lt;p>&lt;strong>判负组 3：&lt;code>list_projects&lt;/code>。&lt;/strong> T1-T3 对照实验里，三个真实场景（架构概览、图查询、变更影响）它一次都没进过调用路径，而 CLI 里一行就有。典型的&amp;quot;挂在那显得功能全&amp;quot;的工具。&lt;/p>
&lt;p>&lt;strong>唯一可惜的舍弃：&lt;code>semantic_query&lt;/code>。&lt;/strong> 概念级查找（&amp;ldquo;找处理重试的逻辑&amp;quot;这类不精确匹配符号名的需求）是符号搜索真覆盖不了的。但它依赖向量语义边——而语义边恰恰是素材里判定&amp;quot;贬值最快&amp;quot;的那类增值插件。如果哪天真需要，方案是把它做成搜索工具的可选参数挂 vector 索引，不单开工具。&lt;/p>
&lt;p>&lt;strong>留下的：3 个场景。&lt;/strong> 对照实验里立住的三个，恰好都是 codegraph 没有的能力：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-fallback" data-lang="fallback">&lt;span class="line">&lt;span class="cl">get_architecture # 架构概览：Leiden 社区划分、复杂度热点、循环依赖
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">query_graph # 图查询：写 Cypher 做&amp;#34;谁调了 X&amp;#34;、跨模块模式确认
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">detect_changes # 变更影响：git diff → 受影响符号的传递集（提交前自检）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"># 另有两个排查辅助留在 CLI 侧，不进 MCP：
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">index_status # 索引新鲜度——结果失真前先看这个
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">check_index_coverage # 覆盖度审计——parse_partial 区域的图结果不可信
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;blockquote>
&lt;p>这 3 个能立住不是偶然：get_architecture 的 Leiden 社区和 detect_changes 的传递影响集是&lt;strong>图算法&lt;/strong>，tree-sitter 解析完之后的加工增值；而符号级查询本质是&amp;quot;更贵的 grep&amp;rdquo;，模型演进越快这类工具越容易被原生搜索追平。不贬值的是图算法、覆盖度审计、跨会话索引资产——裁剪就是留下不贬值的。&lt;/p>
&lt;/blockquote>
&lt;h2 id="中间档scout-和-arch为什么不选">中间档：scout 和 arch，为什么不选
&lt;/h2>&lt;hr>
&lt;blockquote>
&lt;p>裁剪不是只有&amp;quot;全留&amp;quot;和&amp;quot;全退&amp;quot;两个选项。cbm 官方有 tool-profile，自编译还能加自定义档，我在这两档之间停留过，最后还是走了 CLI。&lt;/p>
&lt;/blockquote>
&lt;p>scout 档是零改动方案，一行配置：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;span class="lnt">8
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-json" data-lang="json">&lt;span class="line">&lt;span class="cl">&lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;mcpServers&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;codebase-memory-mcp&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;command&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="s2">&amp;#34;codebase-memory-mcp&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="nt">&amp;#34;args&amp;#34;&lt;/span>&lt;span class="p">:&lt;/span> &lt;span class="p">[&lt;/span>&lt;span class="s2">&amp;#34;--tool-profile=scout&amp;#34;&lt;/span>&lt;span class="p">]&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>arch 档官方没给，要自己编译——在 &lt;code>src/mcp/mcp.c&lt;/code> 的 &lt;code>mcp_tool_allowed()&lt;/code> 里加一组工具名，约 20 行：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-c" data-lang="c">&lt;span class="line">&lt;span class="cl">&lt;span class="c1">// 思路示意：在白名单函数里加一个档位判断
&lt;/span>&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1">&lt;/span>&lt;span class="k">if&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="nf">strcmp&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">profile&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;arch&amp;#34;&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="o">==&lt;/span> &lt;span class="mi">0&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="p">{&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="k">return&lt;/span> &lt;span class="nf">tool_in&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">tool&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;get_architecture&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;query_graph&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;detect_changes&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl"> &lt;span class="s">&amp;#34;index_status&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;check_index_coverage&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="s">&amp;#34;list_projects&amp;#34;&lt;/span>&lt;span class="p">,&lt;/span> &lt;span class="nb">NULL&lt;/span>&lt;span class="p">);&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="p">}&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>三档的省幅：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>档位&lt;/th>
&lt;th>动作&lt;/th>
&lt;th>定义层 token&lt;/th>
&lt;th>相对现状&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>现状两件套&lt;/td>
&lt;td>—&lt;/td>
&lt;td>~8,600&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>scout 档&lt;/td>
&lt;td>一行配置&lt;/td>
&lt;td>~5,840&lt;/td>
&lt;td>省 ~32%&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>arch 档&lt;/td>
&lt;td>自编译补丁&lt;/td>
&lt;td>~4,550&lt;/td>
&lt;td>省 ~48%&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>退出 MCP&lt;/td>
&lt;td>CLI + skill&lt;/td>
&lt;td>~2,750&lt;/td>
&lt;td>省 68%&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;blockquote>
&lt;p>scout/arch 档对&amp;quot;还想留在 MCP 里&amp;quot;的人是合理阶梯。但它们有个共同问题：&lt;strong>判负的工具只是被藏起来了，定义层的判断负担还在&lt;/strong>——我要持续维护&amp;quot;哪些该露出来&amp;quot;的清单，而 CLI 路线把这个维护成本一次性归零。既然判负证据已经够了，中间档只是延迟同一个结论，我没有停在那。&lt;/p>
&lt;/blockquote>
&lt;h2 id="保留集的工作流cli--skill-配方">保留集的工作流：CLI + skill 配方
&lt;/h2>&lt;hr>
&lt;blockquote>
&lt;p>退出 MCP 之后，cbm 的形态从&amp;quot;server 常驻&amp;quot;变成&amp;quot;按需起进程&amp;rdquo;。三个场景各自有固定配方：&lt;/p>
&lt;/blockquote>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;span class="lnt">3
&lt;/span>&lt;span class="lnt">4
&lt;/span>&lt;span class="lnt">5
&lt;/span>&lt;span class="lnt">6
&lt;/span>&lt;span class="lnt">7
&lt;/span>&lt;span class="lnt">8
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># 场景 1：架构概览——大仓库第一刀，先看事实上的模块切分&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">cbm cli get_architecture --aspects clusters
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># 场景 2：跨模块确认——Cypher 直查&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">cbm cli query_graph --query &lt;span class="s2">&amp;#34;...&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">&lt;span class="c1"># 场景 3：commit 影响半径——提交前跑一次当自检&lt;/span>
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">cbm cli detect_changes --since HEAD~1
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>和 CLI 配套的是把配方烧进一个 skill：skill 里写死&amp;quot;什么信号出现时调哪个 CLI 命令、结果为空怎么排查&amp;quot;。这相当于把&amp;quot;MCP 工具描述&amp;quot;的引导职能从定义层搬到了 skill 文本——区别是 skill 文本只在需要时加载，不占每轮对话。&lt;/p>
&lt;blockquote>
&lt;p>说白了这是一笔交换：放弃 agent 的&lt;strong>自主发现&lt;/strong>（模型自己看到工具描述决定调用），换成&lt;strong>确定性调度&lt;/strong>（skill 规则触发）。实测下来 agent 自主发现 15 个工具的成功率并不高——schema 太长，描述互相干扰，误用和空转不少。确定性调度反而准。&lt;/p>
&lt;/blockquote>
&lt;p>两个 server 的分工随之定型，索引也分成两层：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-fallback" data-lang="fallback">&lt;span class="line">&lt;span class="cl">codegraph = 活层（always-on，保存后 ~0.3s 增量，喂日常符号工作）
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">cbm = 深层（milestone/按需全量，喂架构分析与影响面，用完 daemon stop）
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;p>裁剪后的定义层税单：8,600 → 只剩 codegraph ≈ 2,750 tok，&lt;strong>省 68%&lt;/strong>，且 cbm 的结果侧双份开销也一并消失。&lt;/p>
&lt;h2 id="反向参照如果只留-8-个工具理想长什么样">反向参照：如果只留 8 个工具，理想长什么样
&lt;/h2>&lt;hr>
&lt;blockquote>
&lt;p>判负做完，我把两个 server 的能力摊在桌面上重新排列了一次：假如从零设计一个最小工具面，它应该是什么形状。这张表也是裁剪依据的一部分——它划出了&amp;quot;该留的&amp;quot;和&amp;quot;增值插件&amp;quot;的边界。&lt;/p>
&lt;/blockquote>
&lt;p>六个能力层、8 个工具，管理面全部走 CLI 不占 MCP 定义层：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>层&lt;/th>
&lt;th>工具&lt;/th>
&lt;th>接口要点&lt;/th>
&lt;th>对应物&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>L1 定位&lt;/td>
&lt;td>search&lt;/td>
&lt;td>名称/正则/BM25，返回限定元数据&lt;/td>
&lt;td>codegraph search&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>L2 读取&lt;/td>
&lt;td>source&lt;/td>
&lt;td>符号 → 源码+签名+紧邻调用&lt;/td>
&lt;td>codegraph node / explore&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>L3 关系&lt;/td>
&lt;td>relations&lt;/td>
&lt;td>方向(in/out/path)+边类型，游标分页&lt;/td>
&lt;td>codegraph callers/callees/trace&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>L4 变更&lt;/td>
&lt;td>impact / diff_impact&lt;/td>
&lt;td>符号级影响；git diff → 传递影响集&lt;/td>
&lt;td>codegraph impact / cbm detect_changes&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>L5 导览&lt;/td>
&lt;td>architecture&lt;/td>
&lt;td>modules(社区)/cycles/hotspots&lt;/td>
&lt;td>cbm get_architecture（独有）&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>L6 信任&lt;/td>
&lt;td>coverage&lt;/td>
&lt;td>逐文件解析覆盖+索引新鲜度&lt;/td>
&lt;td>cbm check_index_coverage（独有）&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>按短描述+精简 schema 算，8 工具的定义层预算约 3-4KB ≈ 900 tok。对比现状 8,600，省幅在 70% 以上。&lt;/p>
&lt;p>同样重要的是&lt;strong>明确排除&lt;/strong>的清单——这些是调研里判定&amp;quot;贬值最快&amp;quot;的增值插件：&lt;/p>
&lt;div class="highlight">&lt;div class="chroma">
&lt;table class="lntable">&lt;tr>&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code>&lt;span class="lnt">1
&lt;/span>&lt;span class="lnt">2
&lt;/span>&lt;/code>&lt;/pre>&lt;/td>
&lt;td class="lntd">
&lt;pre tabindex="0" class="chroma">&lt;code class="language-fallback" data-lang="fallback">&lt;span class="line">&lt;span class="cl">向量语义边、相似度边、跨仓 intelligence、运行时轨迹注入、
&lt;/span>&lt;/span>&lt;span class="line">&lt;span class="cl">ADR 存图、graph UI、常驻 daemon/协调层
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/td>&lt;/tr>&lt;/table>
&lt;/div>
&lt;/div>&lt;blockquote>
&lt;p>这张表给我的最大启发是&amp;quot;成本前置 vs 成本后置&amp;quot;的分野：cbm 是成本前置 + 捆绑销售（耐用品和易耗品强制一起买），codegraph 是成本后置 + 行为导演。模型演进越快，为弱模型设计的补偿性智能贬值越快——L1 到 L3 那几层本质是&amp;quot;更贵的 grep&amp;quot;，而 L5/L6 依赖的图算法和覆盖度审计不会贬值。裁剪清单和这张表是同一个判断的两个投影。&lt;/p>
&lt;/blockquote>
&lt;h2 id="代价核对判负清单之外的理由">代价核对：判负清单之外的理由
&lt;/h2>&lt;hr>
&lt;blockquote>
&lt;p>工具面裁剪解决 token，但 cbm 的账不止这一页。记录性能侧的实测，作为&amp;quot;为什么不留着试试&amp;quot;的补充证据。&lt;/p>
&lt;/blockquote>
&lt;p>2026-09-04 的一次普通会话里抓到的数字（本机 Windows 11，~1900 文件的 C 项目）：&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>指标&lt;/th>
&lt;th>实测值&lt;/th>
&lt;th>对应上游 issue&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>index worker 内存&lt;/td>
&lt;td>&lt;strong>2.7GB&lt;/strong>（预算 2.8GB 几乎打满）&lt;/td>
&lt;td>#1973：预算是 advisory，宁可 OOM 不降级&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>MCP stdio server&lt;/td>
&lt;td>空闲时段 ~30% 单核&lt;/td>
&lt;td>#1764：每个空闲客户端烧 ~0.7 核&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>二进制体积&lt;/td>
&lt;td>282MB 单个 exe&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>watcher&lt;/td>
&lt;td>每 3~5 秒一轮自触发重索引&lt;/td>
&lt;td>#1953：git 策略对 untracked 文件敏感，工具产物喂它死循环&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>失败 worker&lt;/td>
&lt;td>无退避无限重 fork&lt;/td>
&lt;td>#2015：4h43m 内 2233 个 worker&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>触发链已经实锤：会话工具在仓库里写 &lt;code>.codegraph/&lt;/code> 这类产物目录 → git-strategy watcher 判定 changed → daemon 反复 fork worker。解法之一是把产物目录 gitignore，但更根本的是——&lt;strong>一个&amp;quot;高性能&amp;quot;卖点著称的索引服务，默认配置下会和客户端工具互相喂出死循环&lt;/strong>，这本身就是工具面之外的设计债。&lt;/p>
&lt;blockquote>
&lt;p>cbm 的深度（LSP 类型解析 + 语义边）是用内存和常驻进程换的，codegraph 的增量快是用磁盘和 WAL 换的，没有免费午餐。但&amp;quot;深度&amp;quot;大部分在判负清单里已经卖不出去了——剩下 3 个场景恰恰不依赖常驻 daemon，按需起进程跑完就停，反而是对它资源模型最友好的用法。&lt;/p>
&lt;/blockquote>
&lt;hr>
&lt;blockquote>
&lt;p>裁剪的尽头是一个朴素的判断：MCP 工具面应该只放&amp;quot;agent 每轮都可能自主用到&amp;quot;的东西，低频能力、管理面、被更强工具覆盖的能力，都该退到 CLI。15 个工具筛完剩 3 个场景，token 省 68%，误用率归零（没有可误用的了）——这个比例让我怀疑大部分 MCP 服务器都超配了一倍以上。下一篇写从 MCP 退到 CLI 的过程中暴露的另一类问题：CLI 静默失败——空结果和真失败不可区分，agent 拿着空输出继续推理，比误用工具更危险。所以下一步是给 CLI 做 fail-loud 改造：非法参数报错、空结果带原因、退出码讲真话。&lt;/p>
&lt;/blockquote></description></item></channel></rss>